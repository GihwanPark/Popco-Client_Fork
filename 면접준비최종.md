# POPCO 프로젝트 면접 준비 최종본

> 전체 레포(Admin / Client / Front / Recommended / Infra) 구조 기반으로 작성  
> 기존 면접준비.md 내용 + 전체 아키텍처 이해 통합

---

## 목차

1. [프로젝트 전반 & 아키텍처](#1-프로젝트-전반--아키텍처)
2. [MSA 설계 & 서비스 분리](#2-msa-설계--서비스-분리)
3. [인증 / JWT / OAuth2](#3-인증--jwt--oauth2)
4. [Elasticsearch 검색](#4-elasticsearch-검색)
5. [Redis & 동시성 제어](#5-redis--동시성-제어)
6. [실시간 이벤트 (SSE + WebSocket)](#6-실시간-이벤트-sse--websocket)
7. [추천 시스템 & 페르소나](#7-추천-시스템--페르소나)
8. [AI 연동 (GPT / Gemini)](#8-ai-연동-gpt--gemini)
9. [성능 & 최적화](#9-성능--최적화)
10. [설계 결정 & 트레이드오프](#10-설계-결정--트레이드오프)
11. [Spring & JPA 심화](#11-spring--jpa-심화)
12. [인프라 & 배포 (Docker / Nginx)](#12-인프라--배포-docker--nginx)
13. [장애 대응 & 트러블슈팅](#13-장애-대응--트러블슈팅)
14. [기술 선택 이유 & 대안 비교](#14-기술-선택-이유--대안-비교)
15. [확장성 & 미래 개선 방향](#15-확장성--미래-개선-방향)
16. [꼬리 질문 대비 모음](#16-꼬리-질문-대비-모음)
17. [면접 전 체크리스트](#17-면접-전-체크리스트)

---

## 1. 프로젝트 전반 & 아키텍처

**Q. 프로젝트를 30초 안에 소개해 주세요.**

> POPCO는 사용자의 취향을 7가지 페르소나로 진단하고 AI 기반으로 OTT 콘텐츠를 추천하는 플랫폼입니다. Spring Boot 기반 Admin/Client 서버, FastAPI 기반 AI 추천 엔진, React SPA 프론트로 구성된 MSA 구조입니다. Elasticsearch 복합 필터 검색, Redis 기반 실시간 퀴즈 이벤트, SSE/WebSocket 실시간 알림, GPT/Gemini AI 연동이 주요 기술 포인트입니다.

---

**Q. 전체 아키텍처를 설명해 주세요.**

> 크게 5개 레이어로 구성됩니다.
>
> - **Frontend**: React 19 SPA. Axios로 REST 호출, EventSource로 SSE 구독, STOMP WebSocket으로 게임 진행.
> - **Nginx**: 80포트 단일 진입점. `/api/admin/*` → Admin 서버, `/api/client/*` → Client 서버, `/` → 정적 파일(SPA) 로 라우팅. SSE 경로는 버퍼링 OFF, WebSocket 경로는 Upgrade 헤더 처리.
> - **Application Layer**: Admin(8081) — TMDB 데이터 동기화, 퀴즈·페르소나 관리, 배치. Client(8082) — 사용자 모든 기능. Recommended(8000) — FastAPI AI 추천 엔진.
> - **Data Layer**: MySQL(공유 DB), Redis(토큰·락·퀴즈), Elasticsearch(검색), AWS S3(추천 학습 데이터).
> - **외부 API**: TMDB(콘텐츠), Kakao OAuth2(소셜 로그인), OpenAI GPT(페르소나 멘트), Google Gemini(리뷰 요약).

---

**Q. Admin 서버와 Client 서버를 분리한 이유가 무엇인가요?**

> 두 가지 이유입니다. 첫째, **접근 주체가 다릅니다**. Admin은 내부 운영팀이 사용하고 Client는 일반 사용자가 사용합니다. 인증 방식, 트래픽 패턴, 요구 SLA가 다르기 때문에 분리가 자연스럽습니다. 둘째, **부하 격리**입니다. TMDB 데이터 동기화나 배치 작업은 CPU/메모리를 많이 씁니다. 같은 서버에 두면 사용자 API 응답시간에 영향을 줍니다. 분리하면 Admin에 부하가 몰려도 Client는 정상 서비스됩니다.

---

**Q. Popco-Recommended를 왜 Python/FastAPI로 따로 분리했나요?**

> 두 가지 이유입니다. 첫째, **AI/ML 생태계**입니다. KoBERT, scikit-learn, PyTorch 같은 ML 라이브러리는 Python 생태계가 압도적입니다. Spring으로 같이 올리면 JVM 위에서 Python 라이브러리를 쓰는 우회 방법이 필요하고 유지보수가 어렵습니다. 둘째, **독립 배포·스케일링**입니다. 추천 연산은 CPU/메모리 집약적이라 독립적으로 스케일 아웃이 가능해야 합니다. Client 서버와 Recommended 서버를 분리하면 각각 독립적으로 인스턴스 수를 조절할 수 있습니다.

---

## 2. MSA 설계 & 서비스 분리

**Q. MSA에서 서비스 간 데이터 일관성은 어떻게 유지하나요?**

> POPCO에서는 Admin과 Recommended가 MySQL을 공유하는 방식을 선택했습니다. Admin이 TMDB 데이터를 동기화하면 Recommended와 Client가 같은 DB에서 바로 조회합니다. 완전한 MSA라면 서비스마다 DB를 분리하고 이벤트 기반(Kafka 등)으로 동기화해야 하지만, 팀 규모와 일정을 고려해 공유 DB 방식을 선택했습니다. 이 트레이드오프로 인해 서비스 간 결합도가 높아지는 단점이 있지만, 데이터 불일치 문제는 줄어듭니다.

---

**Q. Nginx를 API Gateway로 사용한 이유가 무엇인가요?**

> 팀 규모에 비해 Spring Cloud Gateway 같은 전용 API Gateway를 도입하면 오버엔지니어링이 됩니다. Nginx는 이미 정적 파일 서빙과 리버스 프록시로 필수였기 때문에, 같은 Nginx 설정에서 경로 기반 라우팅을 추가하는 것이 가장 간단한 방법이었습니다. 인증은 각 서비스의 Spring Security에서 처리합니다. 서비스가 더 많아지거나 Auth 중앙화가 필요해지면 Kong이나 Spring Cloud Gateway로 교체를 고려할 수 있습니다.

---

**Q. SSE와 WebSocket 경로를 Nginx에서 특별히 처리한 이유가 무엇인가요?**

> SSE(`/notifications/stream`)는 HTTP/1.1 기반으로 서버가 무한정 응답을 스트리밍합니다. Nginx 기본 설정에서 `proxy_buffering on`이면 Nginx가 응답을 버퍼에 담다가 한꺼번에 내려보내기 때문에 실시간 이벤트가 지연됩니다. `proxy_buffering off`로 끄면 서버 → 클라이언트로 즉시 전달됩니다. `proxy_read_timeout 24h`는 장시간 연결이 유지될 때 Nginx가 먼저 끊지 않도록 합니다.
>
> WebSocket(`/ws`)은 HTTP에서 프로토콜 업그레이드를 요청합니다. Nginx에서 `Upgrade: websocket`, `Connection: Upgrade` 헤더를 통과시키지 않으면 Nginx가 일반 HTTP 요청으로 처리해 연결이 실패합니다.

---

## 3. 인증 / JWT / OAuth2

**Q. JWT를 선택한 이유가 무엇인가요? 세션 방식과 비교해서 설명해 주세요.**

> JWT는 Stateless 방식이라 서버가 토큰 상태를 저장하지 않습니다. 서버를 수평 확장(Scale Out)할 때 세션 공유 문제가 없습니다. 세션 방식은 요청이 항상 같은 서버로 가야 하거나(Sticky Session), Redis 같은 외부 세션 스토어를 공유해야 합니다. JWT는 토큰 자체에 클레임이 담겨 있어 어느 서버에서든 비밀키로 검증이 가능합니다.
>
> 단점은 발급 후 만료 전까지 강제 무효화가 어렵다는 것입니다. 이를 보완하기 위해 로그아웃 시 Redis BlackList에 Access Token을 등록하고, `JwtAuthenticationFilter`에서 매 요청마다 BlackList 여부를 확인합니다.

---

**Q. Refresh Token은 왜 HttpOnly 쿠키에 저장했나요?**

> JavaScript에서 접근할 수 없도록 하기 위해서입니다. LocalStorage나 sessionStorage에 저장하면 XSS 공격으로 스크립트가 토큰을 탈취할 수 있습니다. HttpOnly 쿠키는 브라우저가 JS 접근을 차단하므로 스크립트 기반 탈취를 막습니다. CSRF는 SameSite=Lax 쿠키 속성과 CORS 정책으로 방어했습니다.

---

**Q. Access Token이 탈취되면 어떻게 대응하나요?**

> 세 가지 방어선을 씁니다.
> 1. **만료 시간을 짧게(15분)** 설정해 탈취되더라도 피해 기간을 제한합니다.
> 2. **로그아웃 시 BlackList 등록**: Redis의 `BlackList` 해시에 Access Token을 TTL(만료 시간까지)과 함께 저장합니다. `JwtAuthenticationFilter`가 매 요청마다 BlackList를 확인해 등록된 토큰이면 401을 즉시 반환합니다.
> 3. **Refresh Token은 HttpOnly 쿠키**: JS 접근이 불가능해 XSS로 탈취할 수 없습니다.

---

**Q. Redis에서 Refresh Token은 어떻게 관리하나요?**

> `Token` Redis 엔티티에 `userId`를 키로 `refreshToken`을 저장합니다. 로그인 시 기존 Token이 있으면 `isRefreshTokenExpiringSoon()`으로 만료 임박 여부를 확인해, 임박했을 때만 재발급하고 여유가 있으면 기존 토큰을 재사용합니다. 로그아웃 시에는 Token 엔트리를 삭제하고 Access Token은 BlackList에 등록합니다.

---

**Q. JwtAuthenticationFilter는 어떻게 동작하나요?**

> Spring Security 필터 체인에 등록된 커스텀 필터입니다.
> 1. `Authorization: Bearer {token}` 헤더를 추출합니다.
> 2. JJWT 라이브러리로 서명을 검증합니다.
> 3. Redis BlackList에 해당 토큰이 있는지 확인합니다.
> 4. 정상이면 `SecurityContextHolder`에 `Authentication` 객체를 설정합니다.
>
> Access Token 없이 `X-Refresh-Token` 헤더만 있는 경우 `ACCESS_TOKEN_EXPIRED` 코드와 함께 401을 반환해 클라이언트가 `/auth/refresh`를 호출하도록 유도합니다.

---

**Q. 카카오 OAuth2 로그인 흐름을 설명해 주세요.**

> 1. 프론트엔드에서 카카오 인증 페이지로 리다이렉트합니다 (`VITE_KAKAO_LOGIN_URL`).
> 2. 사용자가 카카오 계정으로 동의하면 인증코드(code)와 함께 `/auth/kakao/login?code=...`로 리다이렉트됩니다.
> 3. 프론트엔드가 이 code를 백엔드로 전달합니다.
> 4. Client 서버가 카카오 API에 code를 보내 카카오 Access Token을 받습니다.
> 5. 카카오 Access Token으로 사용자 정보(이메일, 닉네임)를 조회합니다.
> 6. 해당 이메일로 DB에 기존 회원이 있으면 로그인, 없으면 자동 회원가입 후 로그인합니다.
> 7. 우리 서비스의 JWT(Access Token + Refresh Token)를 발급해 반환합니다.

---

## 4. Elasticsearch 검색

**Q. MySQL만 쓰면 될 텐데 Elasticsearch를 왜 도입했나요?**

> 두 가지 이유입니다. 첫째, **한국어 자연어 검색**입니다. MySQL LIKE는 형태소 분석을 지원하지 않습니다. "기생충"으로 검색할 때 "기생" + "충"으로 분석해 관련 결과를 찾으려면 Nori 형태소 분석기가 필요합니다. 둘째, **복합 필터 성능**입니다. 장르·평점 범위·플랫폼·연도를 동시에 필터링하는 쿼리는 MySQL 인덱스로는 한계가 있습니다. Elasticsearch는 역인덱스 구조로 이런 다차원 필터에 최적화되어 있습니다.

---

**Q. Elasticsearch 인덱스를 두 개로 나눈 이유가 있나요?**

> `ContentDocument`(검색용)와 `ContentFilterDocument`(필터용) 두 인덱스를 분리했습니다.
>
> 검색 인덱스는 제목·줄거리 같은 `text` 타입 필드에 Nori 분석기를 적용해 TF-IDF 기반 관련도 점수로 동작합니다. 필터 인덱스는 장르·플랫폼 같은 `keyword` 타입 필드에 정확한 값 매칭을 씁니다.
>
> `text` 필드와 `keyword` 필드는 매핑 설계가 근본적으로 다르고, 두 목적을 한 인덱스에 섞으면 분석기 충돌과 쿼리 복잡도가 높아집니다.

---

**Q. MySQL과 Elasticsearch 데이터 동기화는 어떻게 했나요?**

> `ContentIndexingService`와 `ContentMigrationService`를 통해 관리합니다. Admin의 `/index/**` 엔드포인트로 전체 재인덱싱을 수동으로 트리거할 수 있습니다. 서버 시작 시 `ElasticsearchIndexInitializer`(`@PostConstruct`)가 인덱스 존재 여부를 확인합니다.
>
> 실시간 자동 동기화는 구현하지 않았습니다. 콘텐츠 데이터는 TMDB 동기화 배치로 주기적으로 갱신되고, 그 시점에 맞춰 재인덱싱을 실행하는 운영 프로세스로 관리했습니다. 개선 방향으로는 데이터 변경 이벤트를 Kafka로 흘려 ES 인덱스를 자동 갱신하는 CDC 방식이 있습니다.

---

**Q. 필터 기능에서 Elasticsearch와 FastAPI 결과를 어떻게 조합했나요?**

> `ContentFilterService`에서 두 종류의 필터를 교집합으로 합칩니다.
>
> 장르·평점·플랫폼·연도 같은 **정적 필터**는 Elasticsearch `CriteriaQuery`로 처리합니다. 연령대별 인기·페르소나별 인기 같은 **동적 필터**는 FastAPI를 WebClient로 호출해 `ContentId` 목록을 받습니다.
>
> 여러 동적 필터가 동시에 요청되면 `retainAll()`로 교집합을 구성하고, 이 ContentId 목록을 Elasticsearch 정적 필터의 추가 조건으로 합쳐 최종 결과를 만듭니다. 프론트에서 FastAPI를 직접 호출하지 않고 Spring을 거치게 함으로써 두 결과의 조합과 인증을 서버에서 일괄 처리합니다.

---

**Q. Elasticsearch에서 자동완성은 어떻게 구현했나요?**

> `/search/autocomplete` 엔드포인트에서 `prefix` 쿼리 또는 `completion` 타입 필드를 사용합니다. 사용자가 타이핑하는 동안 제목의 앞부분 일치 결과를 반환합니다. 실제 검색과 달리 형태소 분석 없이 `keyword` 타입으로 정확한 prefix 매칭이 필요하기 때문에, 분석기가 적용된 검색 인덱스와 별도로 처리했습니다.

---

## 5. Redis & 동시성 제어

**Q. Redis를 어떤 용도로 사용했나요?**

> 세 가지 용도입니다.
> 1. **JWT 토큰 관리**: `Token` 엔티티(userId → refreshToken)와 `BlackList` 엔티티(invalidated access token, TTL 자동 만료).
> 2. **퀴즈 선착순 처리**: Sorted Set(생존자 순위), String Counter(제출 순서), Hash(퀴즈 진행 상태).
> 3. **분산 락**: `DistributeLockServiceImpl`로 임계 구역 보호. 중복 제출 방지에 사용.

---

**Q. Redis Lua 스크립트를 왜 사용했나요? 트랜잭션으로 처리하면 안 되나요?**

> Java `synchronized`나 DB 트랜잭션은 단일 JVM 프로세스 내에서만 동작합니다. 서버를 수평 확장하면 각 인스턴스가 독립적으로 동작해 동시성을 보장할 수 없습니다.
>
> Redis는 싱글 스레드 이벤트 루프 기반이라 명령 자체가 원자적입니다. Lua 스크립트는 여러 명령을 하나의 원자적 연산으로 묶어 다른 명령이 끼어들 수 없습니다. 퀴즈에서 "중복 참여 확인 → 카운터 증가 → 정원 이내면 Sorted Set 추가"를 하나의 Lua 스크립트로 묶어 Race Condition을 제거했습니다.

---

**Q. 분산 락은 어떻게 구현했나요?**

> `DistributeLockServiceImpl`에서 두 개의 Lua 스크립트로 구현했습니다.
>
> **락 획득**: `EXISTS` 확인 후 없을 때만 `SET key value EX ttl` 원자적 실행.
> **락 해제**: `GET`으로 값 확인 후 자신이 설정한 값일 때만 `DEL`. UUID를 락 값으로 사용해 본인이 획득한 락만 해제합니다.
>
> 퀴즈 답안 제출에서 `quiz:lock:answer:{questionId}:{userId}` 키로 같은 사용자의 중복 제출을 막는 데 사용했습니다.

---

**Q. Redis가 다운되면 서비스가 어떻게 되나요?**

> Redis가 하는 역할별로 영향이 다릅니다. **토큰 관리**가 안 되므로 로그인/로그아웃이 불가능하고, BlackList 확인이 안 되어 로그아웃한 토큰도 유효한 것으로 처리됩니다. **퀴즈 이벤트**는 완전히 멈춥니다. 일반 콘텐츠 조회·리뷰·검색 등 Redis를 직접 사용하지 않는 기능은 계속 동작합니다. 운영 환경에서는 Redis Sentinel이나 Cluster로 고가용성을 확보해야 합니다.

---

**Q. Redis와 DB 중 어느 쪽을 먼저 업데이트하나요?**

> 프로젝트에서 Redis는 토큰 관리와 퀴즈 이벤트 조율에만 사용하고 일반 데이터 캐시로는 활용하지 않았습니다. 토큰 블랙리스트는 **Redis를 먼저** 씁니다. DB 쓰기가 실패해도 Redis에 무효화가 되어 있으면 보안상 안전하기 때문입니다. 반대로 Redis 쓰기가 실패하면 로그아웃이 완전하지 않을 수 있습니다. 이 케이스는 Access Token 만료 시간(15분)이 지나면 자연히 해결됩니다.

---

## 6. 실시간 이벤트 (SSE + WebSocket)

**Q. 퀴즈 이벤트에서 SSE와 WebSocket을 둘 다 사용했는데, 각각 어떤 역할인가요?**

> 역할을 명확히 분리했습니다.
>
> **SSE** (`/notifications/stream`)는 퀴즈 시작 전 **알림** 전용입니다. 서버가 연결된 모든 클라이언트에게 퀴즈 시작 알림을 단방향으로 푸시합니다. `SseNotificationManagementService`가 `ConcurrentHashMap`으로 `SseEmitter` 목록을 관리하고, `HeartbeatScheduler`가 주기적으로 연결 유지 신호를 보냅니다.
>
> **WebSocket** (`/ws`, STOMP 프로토콜)은 퀴즈가 시작된 후 **실시간 게임 진행** 전용입니다. 타이머 카운트다운, 생존자 수 업데이트, 우승자 발표를 `/topic/quiz/{quizId}/...` 채널로 브로드캐스트합니다. `SimpMessagingTemplate`으로 구독 중인 모든 클라이언트에게 동시 전송합니다.
>
> 답안 제출은 HTTP REST(`POST /quizzes/{id}/answers`)로 처리하고, 결과를 WebSocket으로 브로드캐스트합니다.

---

**Q. 퀴즈 게임 진행에 SSE 대신 WebSocket을 선택한 이유가 무엇인가요?**

> 게임 진행 중 메시지 빈도와 종류가 다양합니다. 타이머는 1초마다, 생존자 수는 정답자가 나올 때마다 전송됩니다. WebSocket은 한 번 연결하면 지속적으로 프레임을 주고받을 수 있어 고빈도 스트리밍에 적합합니다.
>
> 또한 STOMP 프로토콜로 `/topic/quiz/{quizId}/question/{questionId}`처럼 채널을 세분화해 클라이언트가 필요한 토픽만 구독할 수 있어 불필요한 메시지를 줄였습니다.
>
> SSE는 구조가 단순해 "퀴즈 시작 알림"처럼 가끔 발생하는 단방향 이벤트에 더 적합합니다.

---

**Q. 답안 제출 동시성을 어떻게 처리했나요?**

> 두 단계로 처리합니다.
>
> 1. **분산 락** (`quiz:lock:answer:{questionId}:{userId}`)으로 같은 사용자의 중복 제출을 막습니다.
> 2. 락 안에서 DB에도 이미 제출한 답안이 있는지 한 번 더 확인합니다 (Double-Check Locking).
>
> 정답인 경우 Redis Lua 스크립트가 원자적으로 순위를 할당합니다. "중복 참여 확인 → 카운터 증가 → Sorted Set에 추가"를 하나의 연산으로 묶어 Race Condition을 방지합니다. 마지막 문제에서 1등이 결정되면 WebSocket으로 우승자 발표를 브로드캐스트합니다.

---

**Q. SSE 연결이 끊기면 어떻게 처리했나요?**

> `SseNotificationManagementService`에서 `SseEmitter`의 `onCompletion`, `onTimeout`, `onError` 콜백을 등록해, 연결이 끊기면 `ConcurrentHashMap`에서 해당 emitter를 즉시 제거합니다. 브로드캐스트 중 전송 실패한 emitter는 `deadConnections` 목록에 모아 루프 완료 후 일괄 제거합니다 (ConcurrentModificationException 방지).

---

**Q. 퀴즈에 수천 명이 동시 접속하면 어떻게 감당하나요?**

> WebSocket 인바운드/아웃바운드 채널 모두 `corePoolSize=10, maxPoolSize=50, queueCapacity=1000`으로 스레드 풀을 설정했습니다. 대규모 이벤트가 예상되면 서버를 수평 확장하고, Redis Pub/Sub으로 인스턴스 간 이벤트를 동기화해야 합니다. `NotificationSubscriber`가 Redis Pub/Sub을 구독하는 구조로 이를 지원합니다. 완전한 스케일 아웃을 위해서는 STOMP 메시지 브로커를 외부 RabbitMQ나 Kafka로 교체하는 것이 이상적입니다.

---

## 7. 추천 시스템 & 페르소나

**Q. 추천 전략을 어떻게 구분해서 적용했나요?**

> 세 가지 전략을 상황에 따라 적용했습니다.
>
> - **일간 인기 콘텐츠**: `DailyPopularContent` 테이블에 배치로 저장된 랭킹 데이터를 반환합니다. 오늘 데이터가 없으면 어제 데이터를 폴백으로 사용합니다.
> - **아이템 기반 추천**: 일간 1위 콘텐츠를 기준으로 `ContentRecommendation` 테이블에서 연관 콘텐츠를 조회합니다.
> - **협업 필터링 / 페르소나·연령대 기반**: FastAPI를 WebClient로 호출해 ContentId 목록을 받고, Elasticsearch 정적 필터와 교집합으로 최종 결과를 구성합니다.

---

**Q. 페르소나는 어떻게 산출하나요?**

> 초기 온보딩 퀴즈(5문항)와 콘텐츠 좋아요/싫어요 피드백으로 산출합니다. `user_personas` 테이블에 사용자별로 7개 페르소나 점수(0.0~100.0)가 저장됩니다. 점수가 가장 높은 것이 메인 페르소나, 두 번째가 서브 페르소나입니다. 메인·서브 점수 차이가 8% 미만이면 "아기 {페르소나명}"으로, 8% 이상이면 성인 페르소나로 표시합니다.
>
> 피드백이 쌓일수록 Recommended 서버가 페르소나 점수를 실시간으로 업데이트합니다.

---

**Q. FastAPI의 추천 알고리즘은 어떤 방식으로 동작하나요?**

> 3계층 알고리즘을 사용합니다.
>
> 1. **페르소나 기반** (`PersonaRecommender`): `user_personas` 점수 상위 2개 페르소나에 연결된 콘텐츠를 가중치 순으로 정렬해 반환합니다.
> 2. **콘텐츠 기반** (`ImprovedMovieRecommendationSystem`): KoBERT로 줄거리를 벡터화하고 TF-IDF + 장르/키워드 유사도를 다층으로 계산합니다. 임베딩 결과는 메모리에 캐싱해 반복 호출 비용을 줄입니다.
> 3. **협업 필터링** (`FilteringRecommender`): 연령대·성별·플랫폼별 인기도 점수로 정렬합니다. 학습 데이터는 AWS S3에 저장하고 boto3로 로드합니다.

---

**Q. 페르소나 진단 온보딩 흐름을 구체적으로 설명해 주세요.**

> 1. 프론트에서 사용자가 5개 질문에 답변하고 콘텐츠 좋아요/싫어요를 선택합니다.
> 2. `POST /personas/onboard`로 Client 서버에 전송합니다.
> 3. Client 서버가 Recommended 서버의 `POST /recommends/personas/onboard`를 내부 HTTP로 호출합니다.
> 4. Recommended 서버가 질문 답변 분석 + 시청이력 기반으로 7개 페르소나 점수를 계산합니다.
> 5. `user_personas` 테이블에 점수를 저장하고 상위 2개 페르소나(Main + Sub)를 결정합니다.
> 6. 해당 페르소나 기반 추천 콘텐츠 목록과 함께 결과를 반환합니다.
> 7. 이후 좋아요/싫어요를 할 때마다 `POST /recommends/personas/feedback`으로 점수가 실시간 업데이트됩니다.

---

## 8. AI 연동 (GPT / Gemini)

**Q. AI 취향 텍스트 생성은 어떻게 구현했나요?**

> `GptService`에서 WebFlux `WebClient`로 OpenAI API를 비동기 호출합니다. 사용자의 상위 2개 페르소나 점수 차이에 따라 프롬프트 템플릿을 선택하고(`{persona1}`, `{genres1}` 변수 치환), OpenAI에 요청 후 응답을 `Mono<PersonaTextResponseDto>`로 반환합니다. 동기 블로킹 없이 처리해 스레드 낭비를 줄였습니다.

---

**Q. 리뷰 요약에 Gemini를 쓰고 페르소나 멘트에 GPT를 쓴 이유가 따로 있나요?**

> 기능별 API 특성을 고려했습니다. 리뷰 요약은 Admin 서버에서 배치로 대량의 텍스트를 처리합니다. Gemini는 긴 텍스트 처리(long context)와 비용 효율 면에서 유리합니다. 페르소나 멘트는 개인화된 창의적인 문장 생성이 필요하고 GPT가 자연스러운 한국어 생성 품질이 높아 GPT를 선택했습니다. 두 AI를 목적에 맞게 분리함으로써 품질과 비용을 최적화했습니다.

---

**Q. AI 리뷰 요약에서 비용을 어떻게 관리했나요?**

> `ReviewSummary` 테이블에 생성된 요약을 저장해 캐시로 활용합니다. 요약이 이미 있으면 DB에서 바로 반환하고, 없거나 리뷰 수가 5개 미만이면 "아직 충분한 리뷰가 모이지 않았어요..." 기본 메시지를 반환합니다. AI 호출은 Admin 배치 스케줄러가 조건을 만족할 때만 수행해 API 비용을 최소화했습니다.

---

## 9. 성능 & 최적화

**Q. N+1 문제는 어떻게 해결했나요?**

> 콘텐츠 목록 조회 시 각 콘텐츠의 좋아요·싫어요 반응을 개별 조회하면 N+1이 발생합니다. `ContentServiceImpl`에서는 해당 페이지의 모든 ContentId를 수집해 `contentReactionRepository.findByUserIdAndContentIds()`로 한 번에 조회하고, `Map<ContentId, Reaction>`으로 변환한 뒤 각 DTO에 O(1)로 적용합니다. 인기 콘텐츠 목록에서도 동일한 패턴을 적용했습니다.

---

**Q. 페이지네이션은 어떤 방식으로 구현했나요?**

> Spring Data의 `Page<T>`를 사용한 offset 기반 페이지네이션입니다. 기본 사이즈는 40건입니다. offset 방식은 데이터가 많아질수록 뒤 페이지에서 `OFFSET N` 만큼 스캔해야 해 성능이 떨어집니다. 데이터 규모가 커지면 마지막으로 조회한 ID를 기준으로 하는 커서 기반(keyset) 페이지네이션으로 개선이 필요합니다.

---

**Q. TMDB 데이터 동기화 시 성능을 어떻게 고려했나요?**

> 두 가지로 최적화했습니다. 첫째, 동기화 작업 자체를 **비동기**로 실행합니다. Admin의 `GET /contents/all`은 즉시 응답을 반환하고, 실제 TMDB API 호출과 DB 저장은 별도 스레드에서 진행됩니다. 둘째, DB 저장 시 **Batch Insert**를 사용합니다. `spring.jpa.properties.hibernate.jdbc.batch_size=50`으로 50건씩 묶어 INSERT하여 DB 왕복 횟수를 줄입니다.

---

**Q. 쿼리 성능 문제가 발생했을 때 어떻게 디버깅했나요?**

> `spring.jpa.show-sql=true`로 실행 쿼리를 확인하고 MySQL `EXPLAIN`으로 실행 계획을 분석했습니다. 동적 조건 쿼리는 QueryDSL로 타입 안전하게 작성해 컴파일 타임에 필드명 오타를 잡고, `BooleanBuilder`로 조건을 조합해 가독성을 높였습니다.

---

## 10. 설계 결정 & 트레이드오프

**Q. 콘텐츠에 복합키(id + type)를 사용한 이유가 무엇인가요?**

> TMDB API에서 영화와 TV 시리즈가 같은 숫자 ID를 가질 수 있습니다. 단순 id만 PK로 사용하면 movie:12345와 tv:12345가 충돌합니다. 복합키 `(content_id, type)`을 사용하면 충돌 없이 두 타입을 동일한 테이블에서 관리할 수 있습니다. 모든 콘텐츠 API가 `/ids/{id}/types/{type}` 형태의 일관된 인터페이스를 유지하는 장점도 있습니다.

---

**Q. Spring에서 FastAPI를 직접 호출하도록 구현한 이유는 무엇인가요?**

> 프론트엔드가 FastAPI를 직접 호출하면 FastAPI 결과와 Elasticsearch 정적 필터의 교집합 처리를 클라이언트가 해야 합니다. Spring을 거치면 서버 내부에서 두 결과를 조합해 단일 응답으로 내려줄 수 있습니다. 인증·인가 처리도 Spring에서 일괄 관리할 수 있어 FastAPI에 별도 인증 로직이 필요 없습니다. CORS 문제도 Spring만 외부에 노출하면 됩니다.

---

**Q. Service Layer를 Interface + Impl로 분리한 이유가 있나요?**

> 주로 테스트 용이성을 위해서입니다. 인터페이스를 기반으로 Mock 객체를 만들어 단위 테스트에서 의존성을 주입할 수 있습니다. Spring AOP(트랜잭션, 로깅)도 인터페이스 기반 프록시로 더 안정적으로 동작합니다.
>
> 다만 프로젝트 내에서 모든 서비스에 일관되게 적용된 것은 아닙니다. `GptService`처럼 구현체가 하나인 경우 인터페이스 없이 구체 클래스로 작성한 경우도 있어, 일관성 측면에서는 개선이 필요합니다.

---

**Q. 신고(Declaration) 시스템은 어떻게 설계했나요?**

> `declarations` 테이블에 신고 대상(CONTENT/REVIEW), 대상 ID, 사유, 처리 상태를 저장합니다. 신고가 누적되면 자동으로 리뷰 status를 BLIND로 변경합니다. 사용자에 대한 제재는 `users.unban_at` 컬럼에 이용 정지 해제 시각을 기록하고, `JwtAuthenticationFilter`에서 `unban_at`이 현재 시각보다 미래인 경우 API 접근을 차단합니다.

---

**Q. Refresh Token 재발급 기준을 어떻게 결정했나요?**

> `JwtUtil.isRefreshTokenExpiringSoon()`으로 만료까지 남은 시간이 threshold 이하일 때 재발급합니다. 매번 Refresh Token을 재발급하면 DB/Redis 쓰기가 불필요하게 발생합니다. 만료 임박 시에만 재발급함으로써 쓰기 비용을 줄이면서도 사용자가 자연스럽게 새 토큰으로 교체되도록 했습니다.

---

## 11. Spring & JPA 심화

**Q. @Transactional을 어디에 어떻게 적용했나요?**

> Service 메서드에 적용했습니다. 읽기 전용 메서드에는 `@Transactional(readOnly = true)`를 사용해 Hibernate dirty checking 대상에서 제외해 성능을 최적화했습니다.
>
> 주의한 점은 self-invocation 문제입니다. `@Transactional` 메서드를 같은 빈 내에서 호출하면 Spring 프록시를 거치지 않아 트랜잭션이 무시됩니다. 이 경우 별도 빈으로 분리하거나 `ApplicationContext`에서 프록시 빈을 꺼내는 방법을 사용했습니다.

---

**Q. QueryDSL을 사용한 이유는요?**

> Elasticsearch `CriteriaQuery`와 함께 동적 필터 쿼리 구성에 활용했습니다. JPQL 문자열은 컴파일 타임에 검증되지 않아 런타임에 오타가 발견됩니다. QueryDSL은 Q클래스를 통해 컴파일 타임 타입 안전성을 보장하고, `BooleanBuilder`로 조건을 동적으로 조합할 때 가독성이 좋습니다.

---

**Q. JPA LAZY 로딩을 사용할 때 주의한 점은요?**

> 모든 연관관계에 기본적으로 `LAZY`를 적용했습니다. LAZY 로딩은 트랜잭션 밖에서 접근하면 `LazyInitializationException`이 발생합니다. 필요한 연관 데이터는 Service 레이어 트랜잭션 안에서 미리 접근하거나, fetch join으로 명시적으로 가져왔습니다. Controller에서 직접 엔티티를 반환하지 않고 항상 DTO로 변환해 영속성 컨텍스트 밖에서의 접근을 방지했습니다.

---

**Q. @EnableScheduling은 어떻게 활용했나요?**

> `@Scheduled`로 일간 인기 콘텐츠 업데이트, 주간 트렌딩 리뷰 집계, 퀴즈 이벤트 타이밍 제어에 사용했습니다. `@EnableScheduling`은 단일 스레드로 동작하기 때문에 스케줄링 태스크가 겹치면 밀립니다. 중요한 배치는 Admin 서버에서만 실행하고, Client를 수평 확장할 때는 Redis 분산 락으로 한 인스턴스만 배치를 실행하도록 보장했습니다.

---

**Q. WebClient를 RestTemplate 대신 선택한 이유는 무엇인가요?**

> FastAPI 호출과 OpenAI API 호출에 WebClient를 사용했습니다. RestTemplate은 블로킹 I/O 방식이라 외부 API 응답을 기다리는 동안 스레드가 점유됩니다. WebClient는 논블로킹 비동기 방식이라 스레드 낭비 없이 다른 요청을 처리할 수 있습니다. 특히 GPT 응답처럼 수 초가 걸리는 외부 API 호출에서 효과가 큽니다. `Mono/Flux`로 반응형 파이프라인을 구성해 응답이 오면 처리됩니다.

---

## 12. 인프라 & 배포 (Docker / Nginx)

**Q. Docker Compose를 어떻게 구성했나요?**

> Popco-Infra 레포에 `docker-compose.yml` 하나로 전체 스택을 관리합니다. mysql, redis, elasticsearch, kibana, popco-admin, popco-client, popco-recommend, nginx 총 8개 서비스가 `popco-network` 브릿지 네트워크에서 실행됩니다. 컨테이너 간 통신은 서비스명 DNS로 직접 연결됩니다(예: `popco-client:8080`). 데이터 지속성은 Named Volume(mysql_data, redis_data, elasticsearch_data)으로 관리합니다.

---

**Q. 개발 환경과 프로덕션 환경을 어떻게 분리했나요?**

> Spring 프로파일로 분리했습니다. `local` 프로파일은 로컬 H2나 로컬 MySQL을 사용하고, `docker` 프로파일은 Docker Compose 내부 서비스명으로 DB/Redis에 연결합니다. 환경변수는 Infra 레포의 `.env` 파일에서 관리하고, `docker-compose.yml`이 `env_file`로 주입합니다. 민감한 값(DB 비밀번호, API 키)은 `.env`에만 있고 코드에 하드코딩되지 않습니다.

---

**Q. Nginx로 SPA를 서빙할 때 특별히 처리한 것이 있나요?**

> `try_files $uri $uri/ /index.html;` 설정이 핵심입니다. React Router가 클라이언트 사이드 라우팅을 사용하기 때문에, `/detail/movie/123` 같은 경로로 직접 접근하면 Nginx가 해당 파일을 찾지 못합니다. `try_files`로 요청한 파일이 없으면 `/index.html`을 반환하도록 해 React Router가 클라이언트에서 라우팅을 처리하게 합니다.

---

**Q. AWS 배포 구성은 어떻게 되어 있나요?**

> DB는 AWS RDS MySQL을 사용하고, 추천 학습 데이터는 S3에 저장합니다. 프론트엔드는 Nginx에서 정적 파일로 서빙합니다. Elasticsearch는 Docker Compose에서 단일 노드로 실행합니다. 환경별로 `.env`의 `DB_HOST`를 RDS 엔드포인트로 바꿔 Docker Compose가 AWS RDS에 연결합니다.

---

## 13. 장애 대응 & 트러블슈팅

**Q. 서비스 운영 중 발생한 버그나 장애 경험이 있나요?**

> 퀴즈 이벤트 테스트 중 동시 제출 시 순위가 뒤바뀌는 문제가 있었습니다. 원인은 "카운터 조회 → 증가 → 저장" 사이에 다른 요청이 끼어드는 Race Condition이었습니다. Redis의 `INCR` 명령과 Lua 스크립트로 이 과정을 원자적으로 묶어 해결했습니다.

---

**Q. Elasticsearch 인덱스가 MySQL과 데이터 불일치가 생기면 어떻게 하나요?**

> Admin 전용 `/index/**` 엔드포인트로 전체 재인덱싱을 트리거합니다. `ContentMigrationService`가 MySQL의 모든 콘텐츠를 읽어 ES 인덱스를 다시 빌드합니다. 현재 수동 재인덱싱 방식이라 운영 체크리스트에 포함시켰습니다. 장기적으로는 변경 이벤트 기반 자동 동기화(CDC, Debezium 등)가 더 안정적입니다.

---

**Q. 외부 API(OpenAI, Kakao, FastAPI) 호출 실패 시 어떻게 처리했나요?**

> `GlobalExceptionHandler`에서 외부 API 예외를 잡아 적절한 에러 메시지를 반환합니다. 구체적으로:
>
> - FastAPI 호출 실패: `WebClient`의 `onErrorResume`으로 실패 시 빈 리스트를 반환해 필터 결과가 없는 응답을 내보냅니다. 핵심 기능(콘텐츠 조회)에 영향을 주지 않습니다.
> - AI 요약 실패: "아직 충분한 리뷰가 모이지 않았어요..." 기본 메시지를 반환합니다.
> - 카카오 API 실패: 로그인 실패 메시지를 반환하고 재시도를 안내합니다.

---

**Q. 로깅 및 모니터링은 어떻게 하나요?**

> Elasticsearch + Kibana를 사용합니다. 애플리케이션 로그를 Elasticsearch에 수집하고 Kibana 대시보드에서 에러 패턴, API 응답 시간, 사용자 활동을 시각화합니다. Spring Actuator로 `/actuator/health`와 `/actuator/metrics`를 노출해 서비스 상태를 모니터링합니다.

---

## 14. 기술 선택 이유 & 대안 비교

**Q. MySQL을 쓰면서 MongoDB 같은 NoSQL은 왜 고려하지 않았나요?**

> POPCO의 도메인은 관계가 촘촘합니다. `User → Review → ReviewReaction`, `Collection → CollectionContent → Content` 처럼 다대다 관계와 외래키 제약이 핵심입니다. 관계형 DB는 JOIN과 트랜잭션으로 이 관계를 자연스럽게 표현합니다. MongoDB를 쓰면 리뷰 좋아요 수 집계나 컬렉션에 포함된 콘텐츠 조회처럼 여러 컬렉션을 조인해야 하는 쿼리가 복잡해집니다.
>
> 단, 페르소나 점수나 리뷰 요약처럼 스키마가 유동적이거나 계층형 JSON을 그대로 저장할 필요가 있는 부분은 MongoDB가 유리합니다. 만약 재설계한다면 콘텐츠 메타데이터(출연진, 키워드)처럼 write가 드물고 read가 집중되는 데이터는 MongoDB로 분리하는 것도 고려할 수 있습니다.

---

**Q. Redis를 Memcached 대신 선택한 이유는 무엇인가요?**

> 두 가지 결정적인 이유가 있습니다. 첫째, **자료구조**입니다. Memcached는 단순 key-value(String)만 지원합니다. POPCO에서는 퀴즈 선착순에 Sorted Set, 퀴즈 진행 상태에 Hash, 카운터에 String INCR을 각각 다른 자료구조로 쓰고 있습니다. Memcached로는 이 모두를 애플리케이션 레이어에서 직접 구현해야 합니다. 둘째, **Lua 스크립트와 Pub/Sub**입니다. 원자적 동시성 제어를 위한 Lua 스크립트, 인스턴스 간 이벤트 동기화를 위한 Pub/Sub은 Redis 전용 기능입니다. Memcached에는 없습니다.

---

**Q. Elasticsearch를 MySQL Full-Text Search 대신 선택한 이유는 무엇인가요?**

> MySQL Full-Text Search는 한국어 형태소 분석을 기본 지원하지 않습니다. 플러그인을 추가해도 분석 품질이 Nori 형태소 분석기보다 낮습니다. 또한 `MATCH ... AGAINST` 문법은 관련도 점수 기반 랭킹은 지원하지만, 배우·장르·평점 범위·연도를 동시에 조합하는 복합 필터와 관련도 점수 정렬을 함께 처리하는 것은 MySQL에서 인덱스 활용이 어렵습니다. ES는 역인덱스 구조로 이런 쿼리에 최적화되어 있고, `BoolQuery`로 복합 조건을 직관적으로 조합할 수 있습니다.
>
> 트레이드오프는 별도 저장소를 운영해야 해서 MySQL과의 동기화 문제가 생긴다는 점입니다. 검색 요구사항이 단순했다면 MySQL로 충분했을 겁니다.

---

**Q. Spring Boot를 Node.js나 다른 프레임워크 대신 선택한 이유는 무엇인가요?**

> 세 가지 이유입니다. 첫째, **JPA + QueryDSL + Spring Data Elasticsearch**처럼 검증된 엔터프라이즈 ORM·검색 통합 생태계가 성숙합니다. Node.js 진영은 비슷한 수준의 ORM 생태계를 갖추려면 조합을 직접 구성해야 합니다. 둘째, **Spring Security**로 JWT 필터 체인, OAuth2 클라이언트, CSRF/CORS 처리를 선언적으로 구성할 수 있습니다. 셋째, **타입 안전성**입니다. Java의 컴파일 타임 타입 검사가 QueryDSL Q클래스와 결합하면 런타임 오류를 사전에 잡을 수 있습니다.
>
> Node.js는 I/O 집약적인 실시간 스트리밍에는 더 유리합니다. WebSocket/SSE 트래픽이 압도적으로 많은 서비스라면 Node.js를 선택했을 수도 있습니다.

---

**Q. AI 추천 엔진에 FastAPI를 선택한 이유는 무엇인가요? Flask나 Django는 왜 아닌가요?**

> **Flask vs FastAPI**: Flask는 동기 WSGI 프레임워크입니다. 추천 요청은 여러 사용자가 동시에 보내는데, Flask에서 비동기 처리를 하려면 Gevent나 Gunicorn 워커 수를 늘려야 합니다. FastAPI는 ASGI 기반 비동기를 네이티브로 지원해 `async def`로 DB 쿼리·S3 호출을 논블로킹으로 처리합니다. **Django vs FastAPI**: Django는 ORM·어드민·폼 처리 등 풀스택 기능을 포함하지만 AI 엔진 서버에는 불필요한 오버헤드입니다. FastAPI는 경량이고 Pydantic으로 요청/응답 스키마를 자동 검증합니다. `/docs` 자동 생성 Swagger도 팀 협업에 편리했습니다.

---

**Q. SSE와 WebSocket 중 알림에 SSE를 선택한 근거를 기술적으로 설명해 주세요.**

> 알림은 **서버 → 클라이언트 단방향**입니다. SSE는 HTTP/1.1 위에서 동작하므로 별도 프로토콜 업그레이드가 필요 없고, 브라우저의 EventSource API가 자동 재연결을 처리합니다. WebSocket은 양방향이 필요할 때 유리하지만, 연결 수립에 HTTP Upgrade 핸드셰이크 비용이 있고 프레임 단위 통신이 단방향 스트리밍보다 오버헤드가 있습니다.
>
> 또한 SSE는 HTTP 인프라(로드밸런서, Nginx, CDN)와 자연스럽게 통합됩니다. Nginx에서 `proxy_buffering off`만 설정하면 동작합니다. WebSocket은 프록시 레이어마다 Upgrade 헤더 처리를 별도 설정해야 합니다.

---

**Q. Docker Compose를 선택했는데 Kubernetes는 왜 고려하지 않았나요?**

> Kubernetes는 운영 자동화(Auto Scaling, Self-healing, Rolling Deploy, ConfigMap/Secret 관리) 측면에서 강력하지만, 학습 비용과 운영 복잡도가 매우 높습니다. 팀이 프로젝트 기간 내에 비즈니스 로직과 인프라를 동시에 완성해야 하는 상황에서 K8s 도입은 비용 대비 효과가 낮았습니다.
>
> Docker Compose는 서비스 의존성(`depends_on`), 환경변수 주입(`env_file`), 네트워크 격리(`popco-network`), 볼륨 관리를 단일 파일로 관리할 수 있어 팀 전체가 동일한 환경을 재현하기 쉽습니다. 서비스가 더 많아지거나 트래픽이 증가해 자동 확장이 필요해지면 K8s 또는 AWS ECS로 전환을 고려할 것입니다.

---

**Q. 메시지 큐(Kafka, RabbitMQ)를 도입하지 않은 이유는 무엇인가요?**

> 현재 서비스 간 통신이 동기 HTTP 호출이라 메시지 큐가 필수는 아니었습니다. 메시지 큐가 필요한 시나리오는 "요청이 급증할 때 처리를 완충"하거나 "서비스 간 완전한 비동기 디커플링"이 필요할 때입니다. POPCO에서 TMDB 동기화는 이미 Admin 서버 내부에서 `@Async`로 비동기 처리하고, FastAPI 추천 호출은 WebClient 논블로킹으로 처리합니다. 트래픽 규모가 커서 Recommended 서버가 Client 서버의 요청을 감당하지 못하는 상황이 된다면, 그때 Kafka를 도입해 추천 요청을 큐에 쌓고 Recommended가 처리 가능한 속도로 소비하는 구조로 전환할 것입니다.

---

## 15. 확장성 & 미래 개선 방향

**Q. 트래픽이 현재의 10배가 된다면 어디가 먼저 병목이 될까요? 어떻게 대응하겠습니까?**

> 세 곳이 순서대로 문제가 됩니다.
>
> 1. **MySQL**: 모든 서비스가 공유하는 단일 DB입니다. 읽기 트래픽이 증가하면 Read Replica를 추가해 읽기 쿼리를 분산합니다. `@Transactional(readOnly = true)` 라우팅과 함께 사용합니다. 쓰기가 병목이면 샤딩(user_id 기준) 또는 테이블 파티셔닝(날짜 기준)을 검토합니다.
> 2. **Recommended 서버**: KoBERT 추론은 CPU 집약적입니다. Horizontal Scale Out + GPU 인스턴스 도입, 추론 결과 캐시(Redis) 적용으로 대응합니다.
> 3. **SSE 연결 수**: SSE는 커넥션 당 스레드를 점유합니다. 수만 개의 동시 SSE 연결을 감당하려면 Spring WebFlux 기반 논블로킹 SSE로 전환하거나, SSE 전용 서버를 분리해 수평 확장합니다.

---

**Q. 현재 구조에서 가장 먼저 개선하고 싶은 부분이 어디인가요?**

> **Elasticsearch 동기화 방식**입니다. 현재 수동 재인덱싱 방식은 운영자가 TMDB 동기화 직후 직접 재인덱싱을 트리거해야 합니다. 이 과정이 누락되면 검색 결과가 최신 콘텐츠를 반영하지 못합니다.
>
> 개선 방안: Admin 서버에서 콘텐츠 저장 후 `@TransactionalEventListener`로 도메인 이벤트를 발행하고, 이를 구독해 ES 인덱싱을 자동 실행합니다. 트래픽이 더 크다면 Kafka로 이벤트를 흘려 ES 컨슈머가 처리하는 CDC 구조로 전환합니다.

---

**Q. 완전한 MSA로 전환한다면 현재 구조에서 무엇을 바꾸겠습니까?**

> 세 가지를 우선적으로 바꿉니다.
>
> 1. **DB 분리**: 현재 Admin, Client, Recommended가 MySQL을 공유합니다. 완전한 MSA라면 서비스마다 독립 DB를 갖고, 변경 이벤트를 Kafka로 발행해 다른 서비스가 자체 DB에 복제하는 방식(CQRS + Event Sourcing)으로 전환합니다.
> 2. **API Gateway 도입**: 현재 Nginx가 경로 기반 라우팅만 합니다. API Gateway(Spring Cloud Gateway 또는 Kong)를 두어 인증, Rate Limiting, 서킷 브레이커를 중앙화합니다. 각 서비스는 도메인 로직만 책임집니다.
> 3. **서비스 디스커버리**: 현재 서비스 간 통신은 하드코딩된 컨테이너명(DNS)으로 합니다. Eureka 또는 Kubernetes Service로 동적 서비스 디스커버리를 추가하면 인스턴스가 늘거나 줄어도 자동으로 라우팅됩니다.

---

**Q. 추천 알고리즘을 더 정교하게 개선한다면 어떤 방향으로 가겠습니까?**

> 세 단계로 개선할 수 있습니다.
>
> 1. **온라인 학습 도입**: 현재 KoBERT 임베딩은 배치 사전 계산입니다. 사용자 피드백이 실시간으로 반영되도록 Apache Kafka Streams로 피드백 이벤트를 흘리고, 모델 가중치를 주기적으로 업데이트합니다.
> 2. **명시적 평점 → 암묵적 피드백 활용**: 현재 좋아요/싫어요만 씁니다. 시청 시간, 스크롤 깊이, 상세 페이지 체류 시간 같은 암묵적 신호를 수집하면 더 정확한 선호도 파악이 가능합니다.
> 3. **A/B 테스트 인프라**: 현재 추천 정확도를 측정하는 메커니즘이 없습니다. 사용자를 그룹으로 나눠 알고리즘 버전을 다르게 노출하고, CTR(클릭률)·완주율·재방문율로 알고리즘 효과를 정량 측정합니다.

---

**Q. 글로벌 서비스로 확장해야 한다면 현재 아키텍처에서 무엇이 문제가 되나요?**

> 네 가지 문제가 있습니다.
>
> 1. **레이턴시**: 서버가 서울 리전에만 있습니다. 미국 사용자는 왕복 지연(RTT)이 200ms 이상 됩니다. CloudFront 같은 글로벌 CDN으로 정적 파일·캐시 가능한 API 응답을 엣지에서 서빙하고, 멀티 리전 배포를 검토합니다.
> 2. **한국어 특화 모델**: KoBERT는 한국어 전용입니다. 다국어 서비스라면 mBERT(Multilingual BERT)나 파국어 SBERT 등 다국어 임베딩 모델로 교체해야 합니다.
> 3. **타임존**: 트렌딩 리뷰 집계, 일간 랭킹 배치가 한국 시간 기준입니다. 글로벌 서비스라면 UTC 기준으로 통일하거나 리전별 배치 타이밍을 분리해야 합니다.
> 4. **법적 요구사항**: GDPR(유럽), CCPA(미국) 대응을 위해 사용자 데이터 보관 기간·삭제 권리 API를 추가해야 합니다.

---

**Q. Kafka를 지금 당장 도입한다면 어느 부분에 먼저 적용하겠습니까?**

> 가장 효과가 큰 두 곳에 적용하겠습니다.
>
> 1. **콘텐츠 동기화 파이프라인**: TMDB 데이터 저장 이벤트를 Kafka 토픽(`content.synced`)으로 발행하면, Elasticsearch 인덱서와 Recommended 서버의 학습 데이터 업데이터가 각자 독립적으로 소비합니다. 현재 Admin이 ES 인덱싱과 Recommended 데이터 갱신을 직접 책임지는 강결합 구조를 디커플링합니다.
> 2. **사용자 행동 이벤트 스트림**: 콘텐츠 좋아요·리뷰 작성·검색어 같은 행동 이벤트를 Kafka 토픽(`user.activity`)으로 흘리면, 추천 엔진이 실시간으로 소비해 페르소나 점수를 업데이트하고, 별도 분석 시스템(ClickHouse, BigQuery)이 동시에 소비해 사용자 행동 분석을 합니다. 현재 Client → Recommended 동기 HTTP 호출을 비동기로 전환해 Client의 응답 시간에서 추천 업데이트 비용을 분리할 수 있습니다.

---

**Q. 현재 배치(`@Scheduled`) 방식의 한계가 무엇이고, 어떻게 개선하겠습니까?**

> 세 가지 한계가 있습니다.
>
> 1. **단일 장애 지점**: Admin 서버가 다운되면 배치가 아예 실행되지 않습니다.
> 2. **스케일 아웃 시 중복 실행**: Admin 서버를 여러 대로 늘리면 모든 인스턴스가 같은 배치를 실행합니다. 현재 Redis 분산 락으로 막고 있지만 근본적인 해결은 아닙니다.
> 3. **실패 재처리 없음**: 배치 도중 외부 API(TMDB, Gemini)가 실패해도 다음 주기까지 재시도하지 않습니다.
>
> 개선 방안: **Spring Batch + Quartz Scheduler** 조합으로 Job 상태·재시도·이력을 DB에 관리합니다. 더 나아가 **AWS EventBridge + Lambda** 또는 **Kubernetes CronJob**으로 배치를 애플리케이션 서버와 완전히 분리하면 배치 실패가 서비스에 영향을 주지 않습니다.

---

**Q. 사용자가 100만 명이 되면 DB 설계에서 어떤 부분이 문제가 되나요?**

> 두 곳이 먼저 문제가 됩니다.
>
> 1. **reviews 테이블**: 사용자당 평균 10개 리뷰라고 하면 1000만 행입니다. `content_id + type` 기준 조회가 많기 때문에 인덱스는 이미 있지만, 리뷰 좋아요 수 `COUNT` 집계 쿼리가 무거워집니다. 해결책은 `like_count` 컬럼을 비정규화해 집계 없이 조회하고(현재 구조에 이미 반영), 주간 트렌딩 집계는 배치로 pre-compute합니다.
> 2. **content_reactions 테이블**: 사용자 × 콘텐츠 조합이라 100만 사용자 × 평균 50개 반응 = 5000만 행입니다. `(user_id, content_id, type)` 복합 인덱스로 단건 조회는 빠르지만, 특정 콘텐츠의 전체 좋아요 수 집계는 느려집니다. 콘텐츠별 집계를 별도 테이블이나 Redis 카운터로 분리하는 것을 고려합니다.

---

## 16. 꼬리 질문 대비 모음

| 주제 | 꼬리 질문 | 답변 포인트 |
|------|-----------|------------|
| JWT | "Access Token 만료 시간을 15분으로 정한 근거는?" | 보안(탈취 피해 최소화)과 UX(너무 잦은 갱신 방지) 사이 트레이드오프. Refresh로 투명하게 갱신 |
| JWT | "토큰 탈취 시나리오를 구체적으로 설명해 보세요" | XSS → LocalStorage 노출 방지(HttpOnly Cookie). MITM → HTTPS. BlackList로 로그아웃 후 사용 차단 |
| Redis | "Redis가 다운되면 서비스가 어떻게 되나요?" | 로그인/로그아웃 불가, 퀴즈 이벤트 중단. 일반 조회는 정상. 고가용성은 Sentinel/Cluster 필요 |
| ES | "인덱스 매핑을 잘못 설계하면?" | ES는 매핑 변경이 불가. 인덱스 삭제 후 재생성 필요. 미리 keyword/text 타입 설계가 중요 |
| ES | "ES가 다운되면?" | 검색과 필터 기능 불가. MySQL 기반 기본 조회는 가능하도록 폴백 로직 고려 가능 |
| WebSocket | "100만 명 동시 접속이면?" | 외부 STOMP 브로커(RabbitMQ/Kafka) + 서버 수평 확장. 현재 구조는 Redis Pub/Sub으로 인스턴스 간 동기화 가능 |
| 페르소나 | "추천 정확도는 어떻게 측정했나요?" | 사용자 피드백(좋아요/싫어요) 비율로 간접 측정. 정밀한 A/B 테스트는 미도입 — 개선 포인트로 언급 가능 |
| N+1 | "BatchSize를 너무 크게 잡으면?" | 메모리 압박, GC 부하 증가. 적정 사이즈는 페이지 크기와 연관 엔티티 수를 고려해 결정 |
| 트랜잭션 | "분산 트랜잭션이 필요한 상황이 있었나요?" | Client DB 저장 + Recommended 페르소나 업데이트는 분산 트랜잭션 영역. 현재는 Recommended 실패 시 재시도 없음 — 개선 포인트 |
| FastAPI | "Spring → FastAPI 호출 레이턴시가 늘어나지 않나요?" | 내부 Docker 네트워크에서 호출이라 수 ms 수준. 병렬 필터 처리 최적화 여지 있음 |
| MSA | "완전한 MSA라면 어떻게 개선하겠나요?" | DB 분리 + Kafka 이벤트 기반 동기화, API Gateway 도입, 서비스 디스커버리, 분산 트레이싱(Jaeger) |
| 복합키 | "복합키는 JPA에서 어떻게 구현했나요?" | `@Embeddable` + `@EmbeddedId`로 ContentId 클래스 정의. 혹은 `@IdClass` 패턴 |
| 배포 | "무중단 배포는 어떻게 하나요?" | 현재 Docker Compose로 `docker-compose up --build`. 무중단은 Blue-Green 배포나 Kubernetes Rolling Update로 개선 가능 |

---

## 17. 면접 전 체크리스트

- [ ] 내가 직접 구현한 코드 파일 3~5개 다시 읽기 (특히 퀴즈 Lua 스크립트, JwtAuthenticationFilter, ContentFilterService)
- [ ] 가장 어려웠던 문제와 해결 과정 STAR 형식으로 정리 (Situation → Task → Action → Result)
- [ ] 기술 선택 이유를 "대안 대비 장단점" 형태로 설명 연습
- [ ] 프로젝트 전체 아키텍처를 화이트보드 없이 말로 설명 연습 (5개 서비스 + Nginx + 데이터 계층)
- [ ] 개선하고 싶은 점 2~3가지 준비 (단순 비판 X, 구체적 방안 포함)
  - ES 자동 동기화 (CDC + Debezium)
  - Recommended 실패 시 분산 트랜잭션 보완 (Saga 패턴)
  - 퀴즈 대규모 확장 시 외부 STOMP 브로커 도입
- [ ] "가장 기억에 남는 기술적 결정"과 그 이유 1가지 준비
- [ ] 프로젝트에서 본인이 기여한 부분의 비중과 어려움을 구체적으로 말할 수 있도록 준비
