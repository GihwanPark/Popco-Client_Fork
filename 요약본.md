# POPCO 백엔드 시스템 요약본

> OTT 콘텐츠 추천 플랫폼 백엔드 (`Popco-Client_Fork`)

---

## 기술 스택

| 분류 | 기술 |
|------|------|
| 프레임워크 | Spring Boot 3.5.3 / Java 17 |
| DB | MySQL (JPA/QueryDSL), Redis, Elasticsearch |
| 검색 | Elasticsearch + 한국어 Nori 형태소 분석기 |
| 인증 | JWT (JJWT 0.12.3) + Kakao OAuth2 |
| 파일 저장 | AWS S3 |
| AI | OpenAI / Google Gemini (리뷰 요약, 페르소나 코멘트) |
| 실시간 | SSE (Server-Sent Events) + WebSocket |
| 빌드 | Gradle |

---

## 디렉토리 구조

```
src/main/java/com/popcoclient/
├── auth/          # JWT 인증, Kakao OAuth2
├── user/          # 회원가입, 프로필, 위시리스트
├── content/       # 콘텐츠 조회, 검색, 필터, 추천
├── review/        # 리뷰 CRUD, 반응, AI 요약, 트렌딩
├── persona/       # 7개 페르소나 진단 & 실시간 업데이트
├── event/         # 퀴즈 이벤트 (실시간 서바이버)
├── collection/    # 사용자 컬렉션 (플레이리스트)
├── declaration/   # 신고 시스템
├── redis/         # Token / BlackList 관리
└── common/        # SecurityConfig, S3, 공통 응답, 예외처리
```

---

## 핵심 기능별 동작 방식

### 1. 인증 (Auth)

**JWT 기반 Stateless 인증**

```
요청 → JwtAuthenticationFilter
         ├─ Authorization 헤더에서 Bearer 토큰 추출
         ├─ 서명 검증 (JJWT)
         ├─ Redis Blacklist 확인 (로그아웃 토큰 차단)
         └─ SecurityContextHolder에 사용자 정보 저장
```

- **Access Token**: 단기 수명 (1시간), userId 클레임 포함
- **Refresh Token**: 장기 수명, HttpOnly 쿠키에 저장 (CSRF 방어)
- **로그아웃**: 토큰을 Redis BlackList에 등록 → 이후 요청 차단

**Kakao 소셜 로그인 흐름**
1. 프론트엔드에서 Kakao 인증 코드 수신
2. 백엔드가 코드로 Kakao 토큰 교환
3. Kakao 프로필 조회 → POPCO 계정 생성/조회
4. POPCO JWT 발급

**주요 엔드포인트**
- `POST /auth/login` — 이메일/비밀번호 로그인
- `POST /auth/refresh` — 액세스 토큰 갱신
- `POST /auth/logout` — 토큰 무효화
- `POST /auth/kakao/login` — Kakao 소셜 로그인

---

### 2. 콘텐츠 (Content)

**데이터 모델 특징**: `contentId + type(MOVIE/TV)` 복합키 → TMDB API의 영화/TV 중복 방지

**4가지 추천 전략**

| 전략 | 설명 | 적용 위치 |
|------|------|-----------|
| 일간/주간 인기 | `DailyPopularContent` 스케줄 배치 업데이트 | 메인 랭킹 |
| 콘텐츠 기반 필터링 | `ContentRecommendation` — 유사 콘텐츠 링크 (줄거리, 장르, 배우) | 관련작 추천 |
| 협업 필터링 | 비슷한 취향 사용자가 좋아한 콘텐츠 | 사용자 맞춤 |
| Popcoism (아이템 기반) | 장르/테마/톤 유사도 기반 | 필터 옵션 |

**Elasticsearch 검색 & 필터**
- `ContentDocument` — MySQL 데이터를 ES 인덱스로 매핑
- 한국어 Nori 형태소 분석기로 검색 품질 향상
- 필터 조건: 장르, 평점 범위, 플랫폼, 출시 연도, 연령대, 페르소나 타입, Popcoism

**주요 엔드포인트**
- `GET /contents` — 전체 콘텐츠 (정렬: 최신/인기/ID)
- `GET /contents/ids/{id}/types/{type}` — 상세 + 사용자 반응
- `GET /contents/filters` — Elasticsearch 복합 필터
- `GET /contents/search/**` — 전문 검색
- `GET /contents/popular/types/{type}` — 인기 랭킹

---

### 3. 페르소나 진단 (Persona)

**7가지 페르소나 타입**

| 페르소나 | 선호 장르 |
|----------|-----------|
| 액션헌터 | 액션, 모험, 스릴러, SF, 전쟁 |
| 무비 셜록 | 미스터리, 범죄, 다큐, 역사 |
| 시네파 울보 | 드라마, 로맨스, 음악 |
| 온기 수집가 | 가족, 애니, 코미디 |
| 이세계 유랑자 | 판타지, SF, 애니 |
| 무서워도본다맨 | 공포, 스릴러 |
| 레트로 캡틴 | 서부, 역사, 음악 |

**페르소나 결정 흐름**
```
1. 최초 진단: 5개 질문 + 40개 콘텐츠 선호도 테스트
   → 메인 페르소나 + 서브 페르소나 배정

2. 실시간 업데이트: 리뷰 작성 / 좋아요 / 싫어요 할 때마다
   → UserPersona 점수 재계산

3. AI 코멘트: OpenAI가 사용자 취향 설명 텍스트 생성
```

- `PersonaDetail`: BABY(아기 POPCO) / ADULT(어른 POPCO) 두 캐릭터 버전
- `UserPersona`: 사용자별 페르소나 점수 동적 관리

**주요 엔드포인트**
- `GET /personas` — 내 페르소나 프로필
- `GET /personas/analysis` — 페르소나 평균 대비 통계
- `GET /personas/question/{questionNumber}` — 진단 질문
- `GET /personas/texts` — AI 생성 취향 코멘트

---

### 4. 리뷰 & 평점 (Review)

**데이터 흐름**
```
사용자 리뷰 작성 (0.5~5.0점)
  → Review 저장 (contentId + type + userId 복합키)
  → 리뷰 5개 이상 누적 시 AI 요약 자동 생성 (ReviewSummary)
  → 주간 인기 리뷰 집계 (TrendingReview)
```

- **소프트 삭제**: 삭제 대신 `status` 필드로 상태 관리 (ACTIVE / REPORTED / BLOCKED)
- **반응**: `ReviewReaction`으로 좋아요/취소 추적
- **AI 요약**: 임계값 달성 시 OpenAI/Gemini 호출, `ReviewSummary`에 캐시

**주요 엔드포인트**
- `GET/POST /reviews/contents/{contentId}/types/{type}` — 목록 조회 / 작성
- `PUT/DELETE /reviews/{reviewId}` — 수정 / 삭제
- `POST /reviews/{reviewId}/reaction` — 좋아요
- `GET /reviews/weekly-trend` — 트렌딩 리뷰
- `GET /reviews/summary/contents/{contentId}/types/{type}` — AI 요약
- `GET /reviews/my/**` — 내 리뷰 이력 & 통계

---

### 5. 실시간 퀴즈 이벤트 (Quiz Event)

**가장 복잡한 기능** — Redis Lua 스크립트 + SSE 브로드캐스트 + 서바이버 랭킹

**시스템 구조**
```
[10분 전] SSE로 팝업 알림 전송 (SseNotificationService)
    ↓
[이벤트 시작] 스케줄러가 첫 번째 문제 브로드캐스트
    ↓
[진행 중] SSE로 실시간 전송:
  - 현재 문제 + 선택지
  - 생존자 수 (실시간)
  - 남은 시간 (카운트다운)
  - 생존자 랭킹
    ↓
[정답 제출] EventServiceImpl.submitAnswer()
  → Redis Lua 스크립트로 원자적 처리 (분산 환경 경쟁 조건 방지)
  → 제출 순위를 UserQuizAnswer에 저장
    ↓
[다음 라운드] 정답자만 다음 문제로 진행
```

**동시성 제어**: Redis Lua 스크립트가 "먼저 제출한 사람이 이긴다" 규칙을 원자적으로 처리 → 수만 명 동시 접속에도 정확한 순위 보장

**주요 엔드포인트**
- `GET /quizzes/alarm` — 오늘 퀴즈 여부 + 시작까지 남은 시간
- `GET /quizzes/{quizId}/question/{questionNum}` — 문제 조회
- `POST /quizzes/{quizId}/questions/{questionId}` — 정답 제출
- `GET /quizzes/{quizId}/questions/{questionId}/status` — 현재 생존자 수 + 시간
- `GET /quizzes/{quizId}/questions/{questionId}/survivors` — 생존자 랭킹
- `SSE /events/subscribe/{quizId}` — 실시간 이벤트 스트림 구독

---

### 6. 컬렉션 (Collection)

사용자가 직접 만드는 **콘텐츠 플레이리스트**

- `Collection` — 제목, 설명, 저장 수, 콘텐츠 수
- `CollectionContent` — 순서 포함 컬렉션-콘텐츠 연결
- `MarkedCollection` — 다른 사용자 컬렉션 저장

**주요 엔드포인트**
- `GET /collections/popular/weekly` — 주간 인기 컬렉션
- `POST /collections/{collectionId}/mark` — 컬렉션 저장

---

### 7. 공통 아키텍처 패턴

**응답 형식**: 모든 API는 `ApiResponse<T>` 래퍼로 통일

```json
{
  "success": true,
  "message": "...",
  "data": { ... }
}
```

**주요 설계 결정**

| 결정 | 이유 |
|------|------|
| MySQL + Elasticsearch 병용 | MySQL은 구조적 데이터, ES는 한국어 검색 + 복합 필터 |
| Redis Lua 스크립트 | 퀴즈 이벤트 동시성 제어 (분산 락) |
| SSE (WebSocket 대신) | 서버→클라이언트 단방향 알림에 더 단순하고 자동 재연결 지원 |
| 복합키 (contentId + type) | TMDB 영화/TV 데이터 중복 방지 |
| ReviewSummary 별도 엔티티 | OpenAI API 중복 호출 방지 (캐시) |
| 소프트 삭제 (status enum) | 리뷰 복구 및 관리 가능, 감사 추적 유지 |
| Interface + Impl 패턴 | 서비스 레이어 추상화 및 테스트 용이성 |

---

## 전체 흐름 요약

```
신규 유저
  → 회원가입 / Kakao 로그인
  → 페르소나 진단 (5문항 + 40개 선호도)
  → 메인 화면: 일간 인기, 페르소나 맞춤 추천

콘텐츠 탐색
  → 검색 (Elasticsearch 한국어 검색)
  → 필터 (장르/평점/플랫폼/페르소나/Popcoism)
  → 상세 페이지 (캐스트, 스트리밍 플랫폼, 관련작 추천)

유저 활동
  → 리뷰 작성 → 페르소나 점수 실시간 반영
  → 컬렉션 생성/저장
  → 위시리스트 추가
  → 주간 퀴즈 이벤트 참여 (서바이버 방식)
```

---

## 주요 파일 경로

| 역할 | 파일 |
|------|------|
| 앱 진입점 | `PopcoClientApplication.java` |
| 보안 설정 | `common/config/SecurityConfig.java` |
| JWT 처리 | `auth/jwt/JwtProvider.java`, `auth/filter/JwtAuthenticationFilter.java` |
| 콘텐츠 API | `content/controller/ContentController.java` |
| ES 필터 | `content/controller/ContentFilterController.java` |
| 페르소나 | `persona/controller/personaController.java` |
| 퀴즈 이벤트 | `event/service/impl/EventServiceImpl.java` |
| 리뷰 | `review/controller/ReviewController.java` |
| 공통 응답 | `common/response/ApiResponse.java` |
