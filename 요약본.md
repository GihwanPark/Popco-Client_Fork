# POPCO-Client 프로젝트 요약본

## 1. 프로젝트 개요

**POPCO**는 사용자의 OTT 취향을 분석해 콘텐츠를 추천해주는 백엔드 서비스다.  
Java 17, Spring Boot 3.5.3, Gradle 빌드. 총 Java 파일 약 299개, 14개 모듈.

---

## 2. 기술 스택

| 영역 | 기술 |
|------|------|
| 웹 프레임워크 | Spring Boot 3.5.3, Spring MVC, Spring WebFlux |
| 보안/인증 | Spring Security, JWT (JJWT 0.12.3), Kakao OAuth2 |
| DB (주) | MySQL + Spring Data JPA + Hibernate |
| DB (검색) | Elasticsearch |
| DB (캐시/분산락) | Redis + Lua 스크립트 |
| ORM/쿼리 | JPA, QueryDSL 5.0.0 |
| 실시간 통신 | WebSocket (STOMP), SSE (Server-Sent Events) |
| 클라우드 스토리지 | AWS S3 |
| AI 연동 | OpenAI GPT (취향 텍스트 생성), Gemini (리뷰 요약) |
| FastAPI 연동 | 협업 필터링·연령대·페르소나 추천 (외부 Python 서버) |
| 문서화 | SpringDoc OpenAPI 2.8.6 (Swagger UI) |
| 테스트 | JUnit 5, H2 (인메모리) |

---

## 3. 모듈 구조

```
com.popcoclient
├── auth          - 인증/인가 (JWT, Kakao OAuth2)
├── user          - 회원, 상세 프로필, 찜 목록
├── content       - 콘텐츠(영화/TV) 목록·상세·인기·추천
├── collection    - 사용자 큐레이션 컬렉션
├── review        - 리뷰, 반응, 트렌딩, AI 요약
├── persona       - 7가지 페르소나 진단/분석
├── event         - 주간 퀴즈 이벤트 (WebSocket + SSE)
├── declaration   - 신고
├── chat          - 채팅 (entity/repository만 존재)
├── redis         - JWT 토큰·블랙리스트 캐시
├── exception     - 전역 예외 처리
└── common        - 설정, 공통 응답, S3 서비스
```

---

## 4. 동작 흐름

### 4.1 인증 흐름 (JWT)

**일반 로그인**
```
클라이언트 → POST /auth/login (email, password)
  └→ AuthServiceImpl.login()
       ├→ UserRepository.findByEmail() → User 조회
       ├→ PasswordEncoder.matches() → 비밀번호 검증
       └→ JwtUtil.generateToken(userId)
            ├→ Redis Token 저장 여부 확인
            │   ├→ 기존 Refresh 토큰 있고 만료 임박하지 않으면 재사용
            │   └→ 만료 임박하거나 없으면 새 Refresh 토큰 생성 후 Redis 저장
            ├→ Access Token 생성 (HS256, userId 포함)
            └→ Refresh Token → HttpOnly Cookie로 Set
응답: { accessToken, userId, isProfileComplete, userInfo }
```

**카카오 로그인**
```
클라이언트 → POST /auth/kakao/login?code={인가코드}
  └→ KakaoUtil.requestToken(code) → 카카오 서버에서 AccessToken 수령
  └→ KakaoUtil.requestProfile(token) → 카카오 계정 이메일 추출
       ├→ 기존 유저: 바로 JWT 발급 → "LOGIN" 응답
       └→ 신규 유저: User 생성(비밀번호 null) → JWT 발급 → "SIGNUP" 응답
```

**요청별 인증 처리 (JwtAuthenticationFilter)**
```
모든 HTTP 요청
  └→ JwtAuthenticationFilter.doFilter()
       ├→ Authorization 헤더에서 Bearer 토큰 추출
       ├→ Redis BlackList 확인 (로그아웃된 토큰인지)
       │   └→ 블랙리스트에 있으면 401 반환
       ├→ JwtProvider.validateToken() → 유효하면 SecurityContext에 Authentication 설정
       └→ Access 토큰 없고 X-Refresh-Token 헤더만 있으면 401 (ACCESS_TOKEN_EXPIRED)
```

**토큰 갱신**
```
클라이언트 → POST /auth/refresh (X-Refresh-Token 헤더)
  └→ JwtUtil.refreshAccessToken()
       ├→ Refresh 토큰 서명 검증
       ├→ Redis에서 저장된 Refresh 토큰과 일치 여부 확인
       ├→ 만료 임박하면 새 Refresh 토큰 발급 후 Redis 갱신
       └→ 새 Access 토큰 발급
```

**로그아웃**
```
클라이언트 → POST /auth/logout (Authorization 헤더)
  └→ JwtUtil.logout()
       ├→ Access 토큰 → Redis BlackList에 추가
       └→ Redis에서 해당 userId의 Refresh 토큰 삭제
```

---

### 4.2 콘텐츠 조회 흐름

**콘텐츠 목록 (정렬)**
```
GET /contents?sort=recent|popular|id_asc&userId={id}
  └→ ContentServiceImpl.getAllContents()
       ├→ sort=recent: ContentRepository.findAllByOrderByReleaseDateDesc()
       ├→ sort=popular: ContentRepository.findAllOrderByPopularity()
       └→ userId 있으면 ContentReactionRepository로 해당 페이지 콘텐츠의
          좋아요/싫어요 상태를 한 번에 조회 → DTO에 반영 (N+1 방지)
```

**일간/주간 인기 콘텐츠**
```
GET /contents/popular/types/{type}
  └→ ContentServiceImpl.getDailyPopularContentList()
       ├→ DailyPopularContentRepository.findByBatchContentTypeAndRankedDate(오늘)
       │   └→ 오늘 데이터 없으면 어제 데이터 조회 (배치 미실행 대비)
       ├→ 장르 ID 모아서 GenreRepository 단건 조회 → 이름 합치기
       └→ userId 있으면 ContentReaction 조회 → 좋아요/싫어요 반영
```

**콘텐츠 상세**
```
GET /contents/ids/{id}?type={movie|tv}
  └→ ContentServiceImpl.getContentDetail()
       ├→ ContentId(id, type) 복합키로 Content 조회
       ├→ content.getGenreIds()로 Genre 목록 조회
       └→ userId 있으면 ContentReaction 조회 → 반응 타입 반영
```

**아이템 기반 추천 (1위 관련 콘텐츠)**
```
GET /contents/recommendations?type={movie|tv}
  └→ ContentServiceImpl.getContentRecommendList()
       ├→ DailyPopularContentRepository.findFirstRanked() → 오늘의 1위 콘텐츠 조회
       └→ ContentRecommendationRepository로 해당 콘텐츠와 유사한 콘텐츠 목록 조회
            └→ userId 있으면 사용자 반응 포함, 없으면 반응 없이 반환
```

---

### 4.3 콘텐츠 필터링 흐름 (Elasticsearch + FastAPI 연동)

```
GET /filter/contents?contentType=&genres=&platforms=&minRating=&...
  └→ ContentFilterService.filterContents()
```

**정적 필터** (Elasticsearch Criteria 직접 구성)
- `contentType`, `genres`, `platforms`, `minRating/maxRating`, `minReleaseYear/maxReleaseYear`
- 각 조건을 `Criteria.and()`로 연결 → `CriteriaQuery` 생성

**동적 필터** (FastAPI 외부 호출)
1. `ageGroupFilter` 있으면 → FastAPI `/recommends/filters/popular-by-age-group` 호출
2. `personaFilter` 있으면 → FastAPI `/recommends/filters/personas/popular-by-persona/{id}` 호출
3. `popcorithmFilter` 있으면 → FastAPI `/recommends/popcorithms/users/{userId}/limits/{limit}` 호출
4. 여러 동적 필터가 동시에 있으면 **교집합** 처리 (`retainAll`)

**최종 쿼리 조합**
```
동적 필터 없음: 정적 필터만 → Elasticsearch 쿼리, releaseDate DESC 정렬
동적 필터 있음: (동적 ContentId 목록) AND (정적 조건) → ES 쿼리
              → ES 결과를 FastAPI 점수 기준으로 재정렬
```

각 결과에 로그인 사용자의 좋아요/싫어요 상태 추가 후 반환.

---

### 4.4 페르소나 시스템 흐름

**초기 페르소나 진단**
```
GET /personas/questions/{number}
  └→ PersonaServiceImpl.getPersonaQuestion()
       └→ PersonaQuestionRepository.findBySortOrder(number) → 질문 + 선택지 반환
```

**나의 페르소나 조회**
```
GET /personas
  └→ PersonaServiceImpl.getPersona(userId)
       ├→ UserPersonaRepository.findTop2ByUserPersonaId_UserIdOrderByScoreDesc()
       │   → 1위, 2위 페르소나 점수 조회
       ├→ UtilServiceImpl.calculatePercentages(score1, score2) → 비율 계산
       └→ 1위-2위 점수 차이가 8% 미만이면 "아기 {페르소나명}" + 아기 이미지
          8% 이상이면 "{페르소나명}" + 어른 이미지로 결정
```

**페르소나 분석 (나 vs 같은 페르소나 유저)**
```
GET /personas/analysis
  └→ PersonaServiceImpl.getPersonaAnalysis(userId)
       ├→ 전체 유저 중 나와 같은 메인 페르소나 유저 목록 추출
       ├→ UserDetail로 성별 분포, 연령대 분포 계산
       ├→ Review 평균 점수 비교 (나 vs 페르소나 그룹)
       ├→ 퀴즈 참여율 비교
       └→ 이달의 리뷰 수 비교 반환
```

**AI 취향 텍스트 생성 (OpenAI)**
```
GET /personas/text (비동기)
  └→ GptService.getPersonaText(userId)
       ├→ UserPersonaRepository로 상위 2개 페르소나 정보 조회
       ├→ 페르소나 점수 차이에 따라 프롬프트 템플릿 선택 (단일/복합)
       │   └→ {persona1}, {genres1}, {tag1}, {description1} 등 변수 치환
       ├→ WebClient (WebFlux)로 OpenAI API 비동기 호출
       └→ JSON 응답 파싱 → PersonaTextResponseDto 반환 (Reactor Mono)
```

---

### 4.5 리뷰 흐름

**리뷰 작성**
```
POST /reviews/contents/{contentId}/types/{type}
  └→ ReviewServiceImpl.insertReview()
       ├→ Content 존재 확인, 중복 리뷰 확인 (AlreadyReviewedException)
       ├→ Review.of(request, user, content) → DB 저장
       └→ updateContentStats(): 전체 리뷰 수, 평균 점수 재계산 → Content 업데이트
```

**리뷰 좋아요 (토글)**
```
POST /reviews/{reviewId}/reaction
  └→ ReviewServiceImpl.reactionReview()
       ├→ ReviewReaction 이미 있으면: 카운트 감소 + 삭제
       └→ 없으면: 카운트 증가 + ReviewReaction 저장
```

**AI 리뷰 요약 조회**
```
GET /reviews/summary/contents/{contentId}/types/{type}
  └→ ReviewServiceImpl.getContentReviewSummary()
       ├→ ReviewSummaryRepository.findByContent(content) 조회
       └→ 없으면: "아직 충분한 리뷰가 모이지 않았어요..." 반환
          있으면: AI 요약 텍스트 + 평가 타입 반환
          (요약은 배치 스케줄러가 5개 이상 리뷰 쌓이면 Gemini/OpenAI로 생성해 저장)
```

---

### 4.6 주간 퀴즈 이벤트 흐름

#### 전체 이벤트 아키텍처
```
SSE: 퀴즈 알림, 대기 타이머 브로드캐스트
WebSocket (STOMP): 실시간 퀴즈 진행 상황, 타이머, 우승자 발표
Redis: 선착순 처리 (Sorted Set, Lua 스크립트), 분산락
```

**퀴즈 알림 확인**
```
GET /quizzes/alarm
  └→ QuizServiceImpl.getQuizAlarm()
       ├→ 오늘(Asia/Seoul 기준) 시작되는 Quiz 조회
       └→ 시작 1분 전이면 showPopup=true 반환
```

**SSE 연결 (알림 수신)**
```
GET /sse/connect?clientId={id}
  └→ SseNotificationService.createEmitter(clientId)
       ├→ ConcurrentHashMap<clientId, SseEmitter> 관리
       ├→ 연결 즉시 "connect" 이벤트 전송
       └→ 30분 timeout, 완료/오류 시 자동 제거
HeartbeatScheduler가 주기적으로 모든 emitter에 "heartbeat" 이벤트 전송
```

**퀴즈 시작 & 문제 진행 (WebSocket)**
```
클라이언트 → WebSocket /ws-quiz (SockJS + STOMP)
  └→ QuizWebSocketInterceptor: 연결 시 JWT 토큰 인증
  └→ 구독: /topic/quiz/{quizId}/waiting → 대기 타이머 수신
  └→ 구독: /topic/quiz/{quizId}/timer → 전체 타이머 수신
  └→ 구독: /topic/quiz/{quizId}/question/{questionId} → 문제별 상태 수신

관리자/배치 → EventServiceImpl.startFirstQuestion(quizId)
  └→ startQuestion(quizId, questionId=1)
       ├→ TaskScheduler로 1초마다 QuizStatusResponseDto 브로드캐스트 시작
       │   → /topic/quiz/{quizId}/question/{questionId} 채널
       └→ 10초 후: 타임아웃 브로드캐스트 → 다음 문제 시작 or 퀴즈 종료
```

**답안 제출 (핵심 - 선착순 + 동시성 제어)**
```
POST /quizzes/{quizId}/answer (body: questionId, optionId)
  └→ EventServiceImpl.submitAnswer(quizId, questionId, userId, optionId)
       │
       ├─ [1단계] 엔티티 검증
       │   User / Quiz / QuizQuestion / QuizOption 존재 확인
       │   UserQuizAttempt 없으면 신규 생성
       │
       ├─ [2단계] 중복 제출 방지 (Redis 분산락)
       │   lockKey = "quiz:lock:answer:{questionId}:{userId}"
       │   DistributeLockServiceImpl.tryLock() → Lua 스크립트 원자적 실행
       │   └→ 락 획득 실패 = 이미 제출 중 → DUPLICATE 반환
       │
       ├─ [3단계] 이미 제출한 답안인지 DB 확인
       │   UserQuizAnswerRepository.existsByAttemptAndQuestionId() → 중복이면 반환
       │
       ├─ [4단계] 오답 처리
       │   selectedOption.getIsCorrect() == false → DB 저장 후 WRONG_ANSWER 반환
       │
       └─ [5단계] 정답 선착순 처리 (Redis Lua 스크립트 원자적 실행)
            Keys: survivorKey(Sorted Set), countKey(카운터), progressKey(Hash)
            Args: userId, submissionTime, firstCapacity
            
            Lua 로직:
              1. ZSCORE(survivorKey, userId) → 중복 참여 체크 → -1 반환
              2. INCR(countKey) → 원자적 순위 할당
              3. 순위 ≤ firstCapacity → ZADD(survivorKey, submissionTime, userId)
                 순위 > firstCapacity → 0 반환 (탈락)
            
            결과:
              qualificationOrder > 0 → 생존! broadcastSurvivorUpdate() 호출
              qualificationOrder == 0 → 선착순 초과 → TOO_LATE
              마지막 문제 + 1등 → announceWinner() → WINNER_ANNOUNCED 브로드캐스트
       └─ 락 해제 (finally)
```

**생존자 순위 조회**
```
GET /quizzes/{quizId}/survivors?questionId=&page=&size=
  └→ EventServiceImpl.getSurvivorRanking()
       └→ Redis Sorted Set ZRANGE → submissionTime 기준 정렬된 생존자 목록 반환
```

---

### 4.7 Elasticsearch 검색 흐름

**자동완성**
```
GET /search/autocomplete?query={keyword}
  └→ ContentSearchService → Elasticsearch prefix/fuzzy 쿼리
       └→ ContentDocument에서 title 필드 기반 자동완성 결과 반환
```

**키워드 검색**
```
GET /search/query?q={keyword}&type=&page=&size=
  └→ ContentSearchService → Elasticsearch multi_match 쿼리
       └→ title, overview 등 다중 필드 검색 → 관련도 점수(score) 순 반환
```

**인덱스 초기화**
```
서버 시작 시 ElasticsearchIndexInitializer (@PostConstruct)
  └→ ContentDocument 인덱스, ContentFilterDocument 인덱스 생성/확인
IndexingController → 수동 재인덱싱 API 제공
```

---

## 5. 주요 Entity 관계

```
User(1) ──(1:1)──> UserDetail (닉네임, 나이, 성별, 프로필 이미지)
User(1) ──(1:N)──> WishList, Collection, Review, ContentReaction
User(1) ──(1:N)──> UserPersona, UserChat, UserQuizAttempt, Declaration

Content (id, type) 복합키 사용
Content(1) ──(1:N)──> CastMember, CrewMember, ContentVideo
Content(1) ──(1:N)──> ContentGenre, WatchProvider
Content(1) ──(1:N)──> Review, ContentReaction, ContentRecommendation

Collection(1) ──(1:N)──> CollectionContent ──(N:1)──> Content

Persona(1) ──(1:N)──> PersonaGenre (페르소나별 장르)
Persona(1) ──(1:N)──> PersonaDetail (아기/어른 이미지)
Persona(1) ──(1:N)──> PersonaQuestion → PersonaOption → Option

Quiz(1) ──(1:N)──> QuizQuestion ──(1:N)──> QuizOption
QuizQuestion(1) ──(1:N)──> UserQuizAnswer ── UserQuizAttempt

Redis:
  Token {userId → refreshToken}
  BlackList {accessToken → ""}
  Quiz 선착순: Sorted Set (survivorKey), String (countKey), Hash (progressKey)
  분산락: String (quiz:lock:*)
```

---

## 6. 보안 설정

**인증 불필요 엔드포인트 (whitelist)**
- 회원가입/이메일 확인: `/users/signup`, `/users/email`
- 로그인/토큰 갱신: `/auth/login`, `/auth/refresh`, `/auth/kakao/**`
- 콘텐츠 조회: `/contents/**`
- 리뷰 읽기: `/reviews/contents/**`, `/reviews/weekly-trend`
- 검색/필터: `/search/**`, `/filter/contents`, `/index/**`
- 컬렉션 읽기: `/collections/*` (일부)
- 문서화: `/swagger-ui/**`, `/v3/api-docs/**`

**인증 필요**: 위 외 모든 엔드포인트

**기타**
- CORS 허용: `http://localhost:5173`, `http://www.popco.site`
- CSRF 비활성화 (Stateless REST API)
- Refresh 토큰: HttpOnly Cookie (`SameSite=Lax`)

---

## 7. FastAPI 연동 구조

Spring Boot가 직접 Python FastAPI 서버를 호출 (WebClient 비동기):

| FastAPI 엔드포인트 | 역할 |
|---|---|
| `/recommends/filters/popular-by-age-group` | 연령대별 인기 콘텐츠 |
| `/recommends/filters/personas/popular-by-persona/{personaId}` | 페르소나별 인기 콘텐츠 |
| `/recommends/popcorithms/users/{userId}/limits/{limit}` | 협업 필터링 추천 |

결과는 `ContentId` 기준으로 Elasticsearch 정적 필터와 **교집합** 처리 후 점수 순 정렬.

---

## 8. API 엔드포인트 요약

| 모듈 | 대표 엔드포인트 |
|------|----------------|
| 인증 | POST `/auth/login`, `/auth/logout`, `/auth/refresh`, `/auth/kakao/login` |
| 회원 | POST `/users/signup`, GET `/users/email`, GET·POST·PUT `/users/details` |
| 콘텐츠 | GET `/contents`, `/contents/ids/{id}`, `/contents/popular/types/{type}`, `/contents/recommendations` |
| 검색 | GET `/search/query`, `/search/autocomplete` |
| 필터 | GET `/filter/contents` |
| 컬렉션 | GET·POST·PATCH·DELETE `/collections`, GET `/collections/search` |
| 리뷰 | GET·POST `/reviews/contents/{id}/types/{type}`, PUT·DELETE `/reviews/{id}`, GET `/reviews/summary/...` |
| 페르소나 | GET `/personas`, `/personas/questions/{number}`, `/personas/analysis`, `/personas/text` |
| 퀴즈 | GET `/quizzes/alarm`, `/quizzes`, `/quizzes/{id}/question/{num}`, POST `/quizzes/{id}/answer` |
| SSE | GET `/sse/connect` |
| 신고 | POST `/declarations` |

---

## 9. 브랜치 전략 및 팀 역할

**브랜치 전략**
```
main (배포) ← dev (통합) ← feat/*, fix/*, refactor/*
```
- 브랜치명: `feat/기능명-이슈번호`
- 커밋: `feat: 작업 내용 (#이슈번호)`
- PR: 본인 브랜치 본인 머지 금지

**팀 역할 (README 기준)**

| 이름 | 담당 |
|------|------|
| 박기환 | PM, 개발 환경, 실시간 퀴즈 이벤트, SSE, 아이템 기반 추천 |
| 김건택 | 배포, CI/CD |
| 변지민 | 페르소나 추천, Elasticsearch 필터, 찜 목록, 콘텐츠 목록, FastAPI |
| 신혜원 | 배치 스케줄링, 리뷰/신고/인증 API, JWT/카카오, OpenAI/Gemini, 콘텐츠 기반 추천 |
