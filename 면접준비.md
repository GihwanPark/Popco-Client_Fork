# POPCO 프로젝트 면접 준비

> 프로젝트 경험 기반 예상 질문 & 답변 모음

---

## 목차
1. [프로젝트 전반](#1-프로젝트-전반)
2. [인증 / JWT](#2-인증--jwt)
3. [Elasticsearch](#3-elasticsearch)
4. [Redis & 동시성 제어](#4-redis--동시성-제어)
5. [실시간 퀴즈 이벤트 (SSE + WebSocket)](#5-실시간-퀴즈-이벤트-sse--websocket)
6. [추천 시스템 & 페르소나](#6-추천-시스템--페르소나)
7. [성능 & 최적화](#7-성능--최적화)
8. [설계 결정 & 트레이드오프](#8-설계-결정--트레이드오프)
9. [Spring & JPA 심화](#9-spring--jpa-심화)
10. [장애 대응 & 트러블슈팅](#10-장애-대응--트러블슈팅)

---

## 1. 프로젝트 전반

**Q. 프로젝트를 간략히 소개해 주세요.**

> POPCO는 OTT 콘텐츠 추천 플랫폼입니다. 사용자의 취향을 7가지 페르소나로 진단하고, 진단 결과를 바탕으로 콘텐츠를 추천합니다. 핵심 기능으로는 Elasticsearch 기반 복합 필터 검색, 협업 필터링·콘텐츠 기반 필터링 추천, FastAPI 연동 추천 엔진, AI 리뷰 요약, SSE와 WebSocket + Redis Lua 스크립트를 활용한 실시간 퀴즈 이벤트가 있습니다.

---

**Q. 본인이 담당한 부분은 무엇인가요?**

> *(본인 담당 파트에 맞게 조정하세요)*
> 저는 [페르소나 진단 시스템 / 퀴즈 이벤트 / 콘텐츠 추천 / 인증] 파트를 주로 담당했습니다. 특히 [어떤 문제를 어떻게 해결했는지] 경험이 가장 인상 깊었습니다.

---

**Q. 가장 어려웠던 기술적 도전은 무엇이었나요?**

> 실시간 퀴즈 이벤트에서 동시성 문제가 가장 까다로웠습니다. 수백 명이 동시에 정답을 제출할 때 선착순 순위를 정확하게 매겨야 했는데, 일반적인 DB 업데이트로는 Race Condition이 발생했습니다. Redis Lua 스크립트를 통해 카운터 증가와 순위 저장을 원자적으로 처리하여 해결했습니다.

---

## 2. 인증 / JWT

**Q. JWT를 선택한 이유가 무엇인가요? 세션 방식과 비교해서 설명해 주세요.**

> JWT는 서버가 상태를 저장하지 않는 Stateless 방식이기 때문에 서버를 수평 확장할 때 세션 공유 문제가 없습니다. 세션 방식은 서버마다 세션 스토어를 공유해야 하거나 Sticky Session을 써야 하는데, JWT는 토큰 자체에 클레임이 담겨 있어 어느 서버에서든 검증이 가능합니다. 다만 JWT는 발급 후 만료 전까지 강제 무효화가 어렵다는 단점이 있어, 로그아웃 시 Redis BlackList에 Access Token을 등록하는 방식으로 보완했습니다.

---

**Q. Refresh Token은 왜 HttpOnly 쿠키에 저장했나요?**

> JavaScript에서 접근할 수 없도록 하기 위해서입니다. LocalStorage에 저장하면 XSS 공격으로 토큰이 탈취될 수 있습니다. HttpOnly 쿠키는 JS에서 읽을 수 없어 스크립트 기반 탈취를 막습니다. CSRF는 SameSite=Lax 쿠키 속성과 CORS 설정으로 방어했습니다.

---

**Q. Access Token이 탈취되면 어떻게 대응하나요?**

> Access Token은 만료 시간을 짧게 설정해 피해를 최소화합니다. 로그아웃 시에는 해당 토큰을 Redis BlackList에 등록해 만료 전에도 사용을 차단합니다. `JwtAuthenticationFilter`에서 매 요청마다 BlackList 여부를 확인하고, 등록된 토큰이면 즉시 401을 반환합니다. Refresh Token은 HttpOnly 쿠키에 보관해 JS 접근을 막습니다.

---

**Q. Redis에서 Refresh Token은 어떻게 관리하나요?**

> `Token` Redis 엔티티에 `userId`를 키로 `refreshToken`을 저장합니다. 로그인 시 기존 Refresh Token이 있으면 만료 임박 여부를 확인해(`isRefreshTokenExpiringSoon`) 임박했을 때만 재발급하고, 여유가 있으면 기존 토큰을 재사용합니다. 로그아웃 시에는 이 Token 엔트리를 삭제하고 Access Token은 BlackList에 등록합니다.

---

**Q. JwtAuthenticationFilter는 어떻게 동작하나요?**

> Spring Security 필터 체인에 등록된 커스텀 필터입니다. 모든 요청에서 `Authorization: Bearer {token}` 헤더를 추출하고, JJWT 라이브러리로 서명을 검증합니다. 이후 Redis BlackList에 해당 토큰이 있는지 확인하고, 정상이면 SecurityContextHolder에 Authentication 객체를 설정합니다. Access Token 없이 `X-Refresh-Token` 헤더만 있는 경우 `ACCESS_TOKEN_EXPIRED` 코드와 함께 401을 반환해 클라이언트가 토큰 갱신 요청을 보내도록 유도합니다.

---

## 3. Elasticsearch

**Q. MySQL만 쓰면 될 텐데 Elasticsearch를 왜 도입했나요?**

> 두 가지 이유가 있습니다. 첫째, 한국어 자연어 검색입니다. MySQL의 LIKE 검색은 형태소 분석을 지원하지 않아 검색 품질이 떨어집니다. Elasticsearch의 Nori 형태소 분석기를 적용하면 검색 정확도가 크게 향상됩니다. 둘째, 복합 필터 쿼리 성능입니다. 장르·평점 범위·플랫폼·연도를 동시에 필터링하는 쿼리는 MySQL 인덱스 활용이 어렵지만 ES는 역인덱스 구조로 이런 쿼리에 최적화되어 있습니다.

---

**Q. Elasticsearch 인덱스를 두 개로 나눈 이유가 있나요?**

> `ContentDocument`(검색용)와 `ContentFilterDocument`(필터용) 두 인덱스를 분리했습니다. 검색은 제목·줄거리 같은 `text` 타입 필드에 분석기를 적용해 관련도 점수 기반으로 동작하고, 필터는 장르·플랫폼 같은 `keyword` 타입 필드에 정확한 값 매칭을 사용합니다. 두 용도의 매핑 설계가 달라 인덱스를 분리했습니다.

---

**Q. MySQL과 Elasticsearch 데이터 동기화는 어떻게 했나요?**

> `ContentIndexingService`와 `ContentMigrationService`를 통해 관리합니다. 어드민이 `/index/**` 엔드포인트로 전체 재인덱싱을 수동으로 트리거할 수 있습니다. 서버 시작 시 `ElasticsearchIndexInitializer`(`@PostConstruct`)가 인덱스 존재 여부를 확인합니다. 실시간 자동 동기화는 구현되지 않아 변경 시 수동 재인덱싱이 필요합니다.

---

**Q. 필터 기능에서 Elasticsearch와 FastAPI 결과를 어떻게 조합했나요?**

> `ContentFilterService`에서 두 종류의 필터를 조합합니다. 장르·평점·플랫폼·연도 같은 정적 필터는 Elasticsearch `CriteriaQuery`로 처리하고, 연령대별 인기·페르소나별 인기·협업 필터링은 FastAPI를 WebClient로 호출해 `ContentId` 목록을 받습니다. 여러 동적 필터가 동시에 요청되면 `retainAll()`로 교집합을 구성하고, 이 ContentId 목록을 조건으로 Elasticsearch 정적 필터와 합쳐 최종 결과를 만듭니다.

---

## 4. Redis & 동시성 제어

**Q. Redis를 어떤 용도로 사용했나요?**

> 세 가지 용도로 사용했습니다.
> 1. **JWT 토큰 관리**: `Token` 엔티티에 userId → refreshToken을 저장하고, `BlackList` 엔티티에 로그아웃된 Access Token을 TTL과 함께 저장합니다.
> 2. **퀴즈 선착순 처리**: Sorted Set으로 생존자 순위를, String 카운터로 제출 순서를, Hash로 진행 상태를 저장합니다.
> 3. **분산 락**: `DistributeLockServiceImpl`을 통해 중복 제출 방지 등 임계 구역을 보호합니다.

---

**Q. Redis Lua 스크립트를 왜 사용했나요? 그냥 트랜잭션으로 처리하면 안 되나요?**

> Java `synchronized`나 DB 트랜잭션은 단일 서버에서만 동작합니다. 서버가 여러 대로 수평 확장되면 각 인스턴스가 독립적으로 동작해 동시성을 보장할 수 없습니다. Redis는 싱글 스레드 기반이라 명령 자체가 원자적이고, Lua 스크립트는 여러 명령을 하나의 원자적 연산으로 묶어 실행합니다. 퀴즈에서 "중복 참여 확인 → 카운터 증가 → 정원 이내면 Sorted Set에 추가"를 하나의 Lua 스크립트로 묶어 Race Condition을 제거했습니다.

---

**Q. 분산 락은 어떻게 구현했나요?**

> `DistributeLockServiceImpl`에서 두 개의 Lua 스크립트로 구현했습니다. 락 획득은 `EXISTS` 확인 후 없을 때만 `SET EX`로 원자적으로 등록하고, 락 해제는 `GET`으로 값이 일치할 때만 `DEL`합니다. 락 값으로 UUID를 사용해 본인이 획득한 락만 해제할 수 있습니다. 퀴즈 답안 제출에서 `quiz:lock:answer:{questionId}:{userId}` 키로 중복 제출을 막는 데 사용했습니다.

---

**Q. Redis와 DB 중 어느 쪽을 먼저 업데이트하나요?**

> 프로젝트에서 Redis는 토큰 관리와 퀴즈 이벤트 조율에만 사용하고 일반 데이터 캐시로는 활용하지 않았습니다. 토큰 블랙리스트는 Redis에 먼저 씁니다. DB 쓰기가 실패해도 Redis에 무효화가 되어 있으면 보안상 안전하기 때문입니다.

---

## 5. 실시간 퀴즈 이벤트 (SSE + WebSocket)

**Q. 퀴즈 이벤트에서 SSE와 WebSocket을 둘 다 사용했는데, 각각 어떤 역할인가요?**

> 두 기술의 역할을 명확히 분리했습니다.
>
> **SSE** (`/sse/connect`)는 퀴즈 시작 전 **알림** 용도로만 사용합니다. 서버가 연결된 모든 클라이언트에게 퀴즈 시작 알림을 단방향으로 푸시합니다. `SseNotificationService`가 `ConcurrentHashMap`으로 SseEmitter 목록을 관리하고, `HeartbeatScheduler`가 주기적으로 연결 유지 신호를 보냅니다.
>
> **WebSocket** (`/ws-quiz`, STOMP 프로토콜)은 퀴즈가 시작된 후 **실시간 게임 진행**에 사용합니다. 타이머 카운트다운, 생존자 수 업데이트, 우승자 발표를 `/topic/quiz/{quizId}/...` 채널로 브로드캐스트합니다. `SimpMessagingTemplate`으로 구독 중인 모든 클라이언트에게 동시 전송합니다.
>
> 답안 제출 자체는 HTTP REST (`POST /quizzes/{id}/answer`)로 처리하고, 그 결과를 WebSocket으로 브로드캐스트합니다.

---

**Q. 퀴즈 게임 진행에 SSE 대신 WebSocket을 선택한 이유가 무엇인가요?**

> 게임 진행 중 메시지 종류가 다양하고 빈도가 높기 때문입니다. 타이머는 1초마다, 생존자 수는 정답자가 나올 때마다 전송됩니다. WebSocket은 한 번 연결하면 지속적으로 프레임을 주고받을 수 있어 이런 고빈도 스트리밍에 적합합니다. 또한 STOMP 프로토콜로 `/topic/quiz/{quizId}/question/{questionId}` 처럼 채널을 세분화해 클라이언트가 필요한 구독만 할 수 있어 불필요한 메시지를 줄였습니다.
>
> 반면 SSE는 구조가 단순해 "퀴즈 시작 알림"처럼 가끔 발생하는 단방향 이벤트에 더 적합합니다.

---

**Q. 답안 제출 동시성을 어떻게 처리했나요?**

> 두 단계로 처리합니다. 첫째, 분산 락(`quiz:lock:answer:{questionId}:{userId}`)으로 같은 사용자의 중복 제출을 막습니다. 둘째, 락 안에서 DB로 이미 제출한 답안이 있는지 한 번 더 확인합니다. 정답인 경우 Redis Lua 스크립트가 원자적으로 순위를 할당합니다. Lua 스크립트는 중복 참여 확인, 카운터 증가, Sorted Set 추가를 하나의 연산으로 묶어 Race Condition을 방지합니다. 마지막 문제에서 1등이 결정되면 WebSocket으로 우승자 발표를 브로드캐스트합니다.

---

**Q. SSE 연결이 끊기면 어떻게 처리했나요?**

> `SseNotificationService`에서 SseEmitter의 `onCompletion`, `onTimeout`, `onError` 콜백을 등록해, 연결이 끊기면 `ConcurrentHashMap`에서 해당 emitter를 즉시 제거합니다. 브로드캐스트 중 전송 실패한 emitter는 `deadConnections` 목록에 모아 일괄 제거합니다.

---

**Q. 퀴즈에 수천 명이 동시 접속하면 어떻게 감당하나요?**

> WebSocket 인바운드/아웃바운드 채널 모두 `corePoolSize=10, maxPoolSize=50, queueCapacity=1000`으로 스레드 풀을 설정했습니다. 대규모 이벤트가 예상되면 서버를 수평 확장하고, Redis Pub/Sub으로 인스턴스 간 이벤트를 동기화해야 합니다. `NotificationSubscriber`가 Redis Pub/Sub을 구독하는 구조로 이를 지원합니다.

---

## 6. 추천 시스템 & 페르소나

**Q. 추천 전략을 어떻게 구분해서 적용했나요?**

> 세 가지 전략을 상황에 따라 적용했습니다.
> - **일간 인기 콘텐츠**: `DailyPopularContent` 테이블에 배치로 저장된 랭킹 데이터를 제공합니다. 오늘 데이터가 없으면 어제 데이터를 폴백으로 사용합니다.
> - **아이템 기반 추천**: 일간 1위 콘텐츠를 기준으로 `ContentRecommendation` 테이블에서 연관 콘텐츠를 조회합니다.
> - **협업 필터링 / 페르소나·연령대 기반**: FastAPI 서버를 WebClient로 호출해 콘텐츠 목록을 받고, Elasticsearch 정적 필터와 교집합으로 최종 결과를 구성합니다.

---

**Q. 페르소나는 어떻게 산출하나요?**

> 초기 진단 퀴즈를 통해 페르소나를 산출합니다. `UserPersona` 테이블에 사용자별로 7개 페르소나 점수가 저장되어 있고, 점수가 가장 높은 것이 메인 페르소나, 두 번째가 서브 페르소나입니다. 메인·서브 점수 차이가 8% 미만이면 "아기 {페르소나명}"으로, 8% 이상이면 성인 페르소나로 표시합니다.

---

**Q. AI 취향 텍스트 생성은 어떻게 구현했나요?**

> `GptService`에서 WebFlux `WebClient`로 OpenAI API를 비동기 호출합니다. 사용자의 상위 2개 페르소나 점수 차이에 따라 프롬프트 템플릿을 선택하고(`{persona1}`, `{genres1}` 등 변수 치환), OpenAI에 요청 후 응답을 `Mono<PersonaTextResponseDto>`로 반환합니다. 동기 블로킹 없이 처리해 스레드 낭비를 줄였습니다.

---

**Q. AI 리뷰 요약에서 비용을 어떻게 관리했나요?**

> `ReviewSummary` 테이블에 생성된 요약을 저장해 캐시로 활용합니다. 요약이 이미 있으면 DB에서 바로 반환하고, 없으면 "아직 충분한 리뷰가 모이지 않았어요..." 메시지를 반환합니다. AI 호출은 배치 스케줄러가 일정 리뷰 수 이상 쌓였을 때만 수행해 API 비용을 최소화했습니다.

---

## 7. 성능 & 최적화

**Q. N+1 문제는 어떻게 해결했나요?**

> 콘텐츠 목록 조회 시 각 콘텐츠의 좋아요·싫어요 반응을 개별 조회하면 N+1이 발생합니다. `ContentServiceImpl`에서는 해당 페이지의 모든 ContentId를 모아 `contentReactionRepository.findByUserIdAndContentIds()`로 한 번에 조회하고, Map으로 변환한 뒤 각 DTO에 적용합니다. 인기 콘텐츠 목록에서도 동일한 방식을 적용했습니다.

---

**Q. 페이지네이션은 어떤 방식으로 구현했나요?**

> Spring Data의 `Page<T>` 인터페이스를 사용한 offset 기반 페이지네이션입니다. 기본 사이즈는 40건입니다. offset 방식은 데이터가 많아질수록 뒤 페이지에서 성능이 떨어지는 단점이 있어, 데이터 규모가 커지면 커서 기반(keyset) 페이지네이션으로 개선이 필요합니다.

---

**Q. 쿼리 성능 문제가 발생했을 때 어떻게 디버깅했나요?**

> `spring.jpa.show-sql=true`로 실행 쿼리를 확인하고, MySQL `EXPLAIN`으로 실행 계획을 분석했습니다. QueryDSL을 사용해 동적 조건 쿼리를 타입 안전하게 작성하고, 컴파일 타임에 필드명 오타를 잡았습니다.

---

## 8. 설계 결정 & 트레이드오프

**Q. 콘텐츠에 복합키(id + type)를 사용한 이유가 무엇인가요?**

> TMDB API에서 영화와 TV 시리즈가 같은 숫자 ID를 가질 수 있습니다. 단순 id만 PK로 사용하면 영화 12345와 TV 12345가 충돌합니다. 복합키 `(id, type)`을 사용하면 충돌 없이 두 타입을 동일한 테이블에서 관리할 수 있습니다. 모든 콘텐츠 API가 `types/{type}` 경로 파라미터를 받는 일관된 인터페이스를 유지할 수 있다는 장점도 있습니다.

---

**Q. Spring에서 FastAPI를 직접 호출하도록 구현한 이유가 무엇인가요?**

> 프론트에서 FastAPI를 직접 호출하면 FastAPI 결과와 Elasticsearch 정적 필터의 교집합 처리를 클라이언트가 해야 합니다. Spring을 거치도록 하면 FastAPI의 동적 추천 결과(ContentId 목록)와 Elasticsearch 정적 필터(장르, 평점, 플랫폼 등)를 서버 내부에서 조합해 단일 응답으로 내려줄 수 있습니다. 인증 처리도 Spring에서 일괄 관리할 수 있어 FastAPI에 별도 인증 로직이 필요 없다는 장점도 있습니다.

---

**Q. Service Layer를 Interface + Impl로 분리한 이유가 있나요?**

> 주로 테스트 용이성을 위해서입니다. 인터페이스를 기반으로 Mock 객체를 만들어 단위 테스트에서 의존성을 주입할 수 있습니다. 다만 프로젝트 내에서 모든 서비스에 적용된 것은 아닙니다. `GptService`처럼 구현체가 하나인 경우 인터페이스 없이 구체 클래스로 작성한 경우도 있어, 일관성 측면에서는 개선이 필요합니다.

---

**Q. Refresh Token 만료 처리는 어떻게 했나요?**

> `JwtUtil.isRefreshTokenExpiringSoon()`으로 만료까지 남은 시간이 threshold 이하면 Refresh Token을 재발급합니다. 로그인과 토큰 갱신(`/auth/refresh`) 모두 이 로직을 적용해 사용자가 자연스럽게 새 Refresh Token으로 교체되도록 했습니다. 재발급 시 기존 토큰은 Redis에서 삭제하고 새 토큰으로 교체합니다.

---

## 9. Spring & JPA 심화

**Q. @Transactional을 어디에 적용했나요? 주의할 점은요?**

> Service 메서드에 적용했습니다. 읽기 전용 메서드에는 `@Transactional(readOnly = true)`를 사용해 Hibernate dirty checking을 생략하고 성능을 최적화했습니다. 주의한 점은 self-invocation 문제입니다. `@Transactional`이 적용된 메서드를 같은 클래스 내부에서 호출하면 Spring 프록시를 거치지 않아 트랜잭션이 동작하지 않으므로, 이 경우 별도 빈으로 분리했습니다.

---

**Q. QueryDSL을 사용한 이유는요?**

> Elasticsearch `CriteriaQuery`와 함께 동적 필터 쿼리 구성에 활용했습니다. QueryDSL은 Q클래스를 통해 컴파일 타임에 필드명 오타를 잡을 수 있고, 조건을 동적으로 조합하는 경우에 `BooleanBuilder`로 가독성 있게 작성할 수 있습니다.

---

**Q. JPA LAZY 로딩을 사용할 때 주의한 점은요?**

> 모든 연관관계에 기본적으로 `LAZY`를 적용했습니다. LAZY 로딩은 트랜잭션 밖에서 접근하면 `LazyInitializationException`이 발생하므로, 필요한 연관 데이터는 Service 레이어 트랜잭션 안에서 미리 접근하거나 fetch join으로 명시적으로 가져왔습니다.

---

**Q. @EnableScheduling은 어떻게 활용했나요?**

> `PopcoClientApplication`에 `@EnableScheduling`을 선언하고 스케줄 컴포넌트에 `@Scheduled`로 작업을 등록했습니다. 일간 인기 콘텐츠 업데이트, 주간 트렌딩 리뷰 집계, 퀴즈 이벤트 타이밍 제어에 사용했습니다. 단일 서버에서만 안전하고 스케일 아웃 시 중복 실행 문제가 생기므로, 중요한 작업에는 Redis 분산 락을 함께 사용해 한 인스턴스만 실행되도록 보장했습니다.

---

## 10. 장애 대응 & 트러블슈팅

**Q. 서비스 운영 중 발생한 버그나 장애 경험이 있나요?**

> 퀴즈 이벤트 테스트 중 동시 제출 시 순위가 뒤바뀌는 문제가 있었습니다. 원인은 "카운터 조회 → 증가 → 저장" 사이에 다른 요청이 끼어드는 Race Condition이었습니다. Redis의 INCR 명령과 Lua 스크립트로 이 과정을 원자적으로 묶어 해결했습니다.

---

**Q. Elasticsearch 인덱스가 MySQL과 데이터 불일치가 생기면 어떻게 하나요?**

> 어드민 전용 `/index/**` 엔드포인트로 전체 재인덱싱을 트리거할 수 있습니다. `ContentMigrationService`가 MySQL의 모든 콘텐츠를 읽어 ES 인덱스를 다시 빌드합니다. 현재는 수동 재인덱싱 방식이라 운영 체크리스트에 포함시켰습니다. 장기적으로는 변경 이벤트 기반의 자동 동기화(CDC 방식)가 더 안정적입니다.

---

**Q. 외부 API(OpenAI, Kakao, FastAPI) 호출 실패 시 어떻게 처리했나요?**

> `GlobalExceptionHandler`에서 외부 API 예외를 잡아 사용자에게 적절한 에러 메시지를 반환합니다. FastAPI 호출은 `WebClient`의 `onErrorResume`으로 실패 시 빈 리스트를 반환해 필터 결과가 없는 응답을 보내도록 처리했습니다. AI 요약은 실패 시 "아직 충분한 리뷰가 모이지 않았어요..." 기본 메시지를 반환해 핵심 기능에 영향이 없도록 했습니다.

---

## 자주 나오는 꼬리 질문 대비

| 주제 | 꼬리 질문 예시 |
|------|----------------|
| JWT | "토큰 탈취 시나리오를 구체적으로 설명해 보세요" |
| Redis | "Redis가 다운되면 서비스가 어떻게 되나요?" |
| ES | "ES 인덱스 매핑을 잘못 설계하면 어떤 문제가 생기나요?" |
| WebSocket | "100만 명 동시 접속이면 어떻게 확장하겠습니까?" |
| 페르소나 | "추천 알고리즘의 정확도는 어떻게 측정했나요?" |
| N+1 | "BatchSize를 너무 크게 잡으면 어떤 문제가 있나요?" |
| 트랜잭션 | "분산 트랜잭션이 필요한 상황이 있었나요?" |
| FastAPI | "Spring에서 FastAPI를 호출하면 레이턴시가 늘어나지 않나요?" |

---

## 면접 전 체크리스트

- [ ] 내가 직접 구현한 코드 파일 3~5개 다시 읽기
- [ ] 가장 어려웠던 문제와 해결 과정 STAR 형식으로 정리
- [ ] 기술 선택 이유를 "대안 대비 장단점" 형태로 설명 연습
- [ ] 프로젝트 전체 아키텍처를 화이트보드 없이 말로 설명 연습
- [ ] 개선하고 싶은 점 2~3가지 준비 (단순 비판이 아닌 구체적 방안 포함)