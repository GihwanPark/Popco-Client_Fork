# POPCO 프로젝트 면접 준비

> 프로젝트 경험 기반 예상 질문 & 답변 모음

---

## 목차
1. [프로젝트 전반](#1-프로젝트-전반)
2. [인증 / JWT](#2-인증--jwt)
3. [Elasticsearch](#3-elasticsearch)
4. [Redis & 동시성 제어](#4-redis--동시성-제어)
5. [실시간 퀴즈 이벤트 (SSE)](#5-실시간-퀴즈-이벤트-sse)
6. [추천 시스템 & 페르소나](#6-추천-시스템--페르소나)
7. [성능 & 최적화](#7-성능--최적화)
8. [설계 결정 & 트레이드오프](#8-설계-결정--트레이드오프)
9. [Spring & JPA 심화](#9-spring--jpa-심화)
10. [장애 대응 & 트러블슈팅](#10-장애-대응--트러블슈팅)

---

## 1. 프로젝트 전반

**Q. 프로젝트를 간략히 소개해 주세요.**

> POPCO는 OTT 콘텐츠 추천 플랫폼입니다. 사용자의 취향을 7가지 페르소나로 진단하고, 진단 결과와 리뷰·반응 데이터를 실시간으로 반영해 콘텐츠를 추천합니다. 핵심 기능으로는 Elasticsearch 기반 복합 필터 검색, 협업 필터링·콘텐츠 기반 필터링 추천, AI 리뷰 요약, SSE와 Redis Lua 스크립트를 활용한 실시간 퀴즈 이벤트가 있습니다.

---

**Q. 본인이 담당한 부분은 무엇인가요?**

> *(본인 담당 파트에 맞게 조정하세요)*
> 저는 [페르소나 진단 시스템 / 퀴즈 이벤트 / 콘텐츠 추천 / 인증] 파트를 주로 담당했습니다. 특히 [어떤 문제를 어떻게 해결했는지] 경험이 가장 인상 깊었습니다.

---

**Q. 가장 어려웠던 기술적 도전은 무엇이었나요?**

> 실시간 퀴즈 이벤트에서 동시성 문제가 가장 까다로웠습니다. 수백 명이 동시에 정답을 제출할 때 "먼저 제출한 사람"의 순위를 정확하게 매겨야 했는데, 일반적인 DB 업데이트로는 Race Condition이 발생했습니다. Redis Lua 스크립트를 통해 카운터 증가와 순위 저장을 원자적으로 처리하여 해결했습니다.

---

## 2. 인증 / JWT

**Q. JWT를 선택한 이유가 무엇인가요? 세션 방식과 비교해서 설명해 주세요.**

> JWT는 서버가 상태를 저장하지 않는 Stateless 방식이기 때문에 서버를 수평 확장할 때 세션 공유 문제가 없습니다. 세션 방식은 서버마다 세션 스토어를 공유해야 하거나 Sticky Session을 써야 하는데, JWT는 토큰 자체에 클레임이 담겨 있어 어느 서버에서든 검증이 가능합니다. 다만 JWT는 발급 후 만료 전까지 강제 무효화가 어렵다는 단점이 있어, 저희는 로그아웃 시 Redis Blacklist에 토큰을 등록하는 방식으로 보완했습니다.

---

**Q. Refresh Token은 왜 HttpOnly 쿠키에 저장했나요?**

> JavaScript에서 접근할 수 없도록 하기 위해서입니다. LocalStorage에 저장하면 XSS 공격으로 토큰이 탈취될 수 있습니다. HttpOnly 쿠키는 JS에서 읽을 수 없고, HTTPS와 함께 사용하면 네트워크 탈취도 어렵습니다. CSRF 공격은 SameSite 쿠키 속성과 CORS 설정으로 방어했습니다.

---

**Q. Access Token이 탈취되면 어떻게 대응하나요?**

> Access Token은 만료 시간을 짧게 (1시간) 설정해 피해를 최소화합니다. 로그아웃 시에는 해당 토큰을 Redis Blacklist에 등록해 만료 전에도 사용을 차단합니다. Refresh Token은 HttpOnly 쿠키에 보관해 JS 접근 자체를 막습니다. 더 강력한 보안이 필요하다면 Refresh Token Rotation(재발급 시 기존 토큰 즉시 무효화)을 추가할 수 있습니다.

---

**Q. JwtAuthenticationFilter는 어떻게 동작하나요?**

> Spring Security의 필터 체인에 등록된 커스텀 필터입니다. 모든 요청에서 `Authorization: Bearer {token}` 헤더를 추출하고, JJWT 라이브러리로 서명을 검증합니다. 이후 Redis Blacklist에 해당 토큰이 있는지 확인하고, 정상이면 SecurityContextHolder에 Authentication 객체를 설정해 이후 컨트롤러에서 인증된 사용자로 처리됩니다. 공개 경로(로그인, 회원가입, 콘텐츠 조회 등)는 SecurityConfig에서 permitAll()로 필터를 우회합니다.

---

## 3. Elasticsearch

**Q. MySQL만 쓰면 될 텐데 Elasticsearch를 왜 도입했나요?**

> 두 가지 이유가 있습니다. 첫째, 한국어 검색입니다. MySQL의 LIKE 검색은 형태소 분석을 지원하지 않아 "액션"으로 검색하면 "액션영화"가 안 나오는 문제가 있었습니다. Elasticsearch의 Nori 형태소 분석기를 적용하면 자연어 검색 품질이 크게 향상됩니다. 둘째, 복합 필터 쿼리 성능입니다. 장르 + 평점 범위 + 플랫폼 + 연도 + 페르소나 타입을 동시에 필터링하는 쿼리는 MySQL에서 인덱스 활용이 어렵지만, ES는 역인덱스 구조로 이런 쿼리에 최적화되어 있습니다.

---

**Q. MySQL과 Elasticsearch 데이터 동기화는 어떻게 했나요?**

> `ContentIndexingService`와 `ContentMigrationService`를 통해 관리합니다. 콘텐츠 데이터가 변경될 때 ES 인덱스를 업데이트하고, 어드민이 `/index/**` 엔드포인트로 전체 재인덱싱을 수동으로 트리거할 수 있습니다. 실시간 동기화가 완벽하지 않아 검색 결과에 짧은 지연이 생길 수 있지만, 콘텐츠 메타데이터는 자주 바뀌지 않아 허용 가능한 수준입니다.

---

**Q. Elasticsearch 인덱스 설계에서 신경 쓴 부분이 있나요?**

> `contents-settings.json`에서 Nori 형태소 분석기와 커스텀 필터를 정의했습니다. 검색 대상 필드(제목, 개요)는 `text` 타입으로, 필터링 대상 필드(장르, 플랫폼)는 `keyword` 타입으로 구분했습니다. `text` 타입은 분석기를 거쳐 검색 정확도를 높이고, `keyword` 타입은 정확한 값 매칭과 집계에 사용했습니다.

---

## 4. Redis & 동시성 제어

**Q. Redis를 어떤 용도로 사용했나요?**

> 세 가지 용도로 사용했습니다.
> 1. **JWT 토큰 관리**: Access Token과 BlackList(로그아웃 토큰)를 TTL과 함께 저장해 만료 시 자동 삭제합니다.
> 2. **퀴즈 이벤트 동시성 제어**: Lua 스크립트로 정답 제출 순위를 원자적으로 처리합니다.
> 3. **분산 락**: `DistributeLockServiceImpl`을 통해 멀티 인스턴스 환경에서 중복 실행을 방지합니다.

---

**Q. Redis Lua 스크립트를 왜 사용했나요? 그냥 트랜잭션으로 처리하면 안 되나요?**

> 일반 DB 트랜잭션이나 Java synchronized 키워드는 단일 서버에서만 동작합니다. 서버가 여러 대로 수평 확장되면 각 인스턴스가 독립적으로 동작해 동시성을 보장할 수 없습니다. Redis는 싱글 스레드 기반이라 명령 자체가 원자적이고, Lua 스크립트는 여러 명령을 하나의 원자적 연산으로 묶어 실행할 수 있습니다. 퀴즈에서 "카운터 증가 → 현재 순위 반환 → 임계값 초과 시 차단"을 하나의 Lua 스크립트로 묶어 Race Condition을 완전히 제거했습니다.

---

**Q. Redis와 DB 중 어느 쪽을 먼저 업데이트하나요? (Cache Invalidation)**

> 저희 프로젝트에서 Redis는 주로 토큰 관리와 이벤트 조율에 사용했고 일반 데이터 캐시로는 활용하지 않았습니다. 만약 캐시를 쓴다면 **Write-Through** 또는 **Cache-Aside** 패턴을 상황에 맞게 선택할 것입니다. 토큰 블랙리스트는 Redis에 먼저 쓰는 것이 안전합니다 — DB 쓰기가 실패해도 Redis에 먼저 무효화되어 있으면 보안상 안전하기 때문입니다.

---

## 5. 실시간 퀴즈 이벤트 (SSE)

**Q. WebSocket 대신 SSE를 선택한 이유가 무엇인가요?**

> 퀴즈 이벤트의 통신 패턴이 서버 → 클라이언트 단방향이었기 때문입니다. 문제 전송, 생존자 수 업데이트, 카운트다운은 모두 서버가 클라이언트에게 보내는 방향입니다. SSE는 이 패턴에 최적화되어 있고, HTTP 위에서 동작해 인프라 설정이 간단합니다. 또한 연결이 끊기면 브라우저가 자동으로 재연결을 시도하는 기능이 내장되어 있습니다. WebSocket은 양방향 통신이 필요한 채팅 같은 경우에 적합하고, 별도 프로토콜 업그레이드와 핸드셰이크가 필요해 복잡도가 높습니다.

---

**Q. SSE 연결이 끊기면 어떻게 처리했나요?**

> SSE는 브라우저의 EventSource API가 연결 끊김을 감지하면 자동으로 재연결을 시도합니다. 서버 측에서는 `SseNotificationManagementService`로 활성 연결 목록을 관리하고, 연결이 끊기면 해당 SseEmitter를 목록에서 제거합니다. 클라이언트가 재연결하면 새 SseEmitter가 등록됩니다. 이벤트 ID를 함께 전송하면 재연결 시 놓친 이벤트를 `Last-Event-ID` 헤더로 요청할 수 있지만, 퀴즈의 실시간 특성상 이전 이벤트 재전송보다는 현재 상태를 새로 받는 방식이 적합했습니다.

---

**Q. 퀴즈에 수천 명이 동시 접속하면 SSE 연결을 어떻게 감당하나요?**

> SSE 연결은 HTTP 롱 폴링과 달리 지속 연결이라 스레드를 점유합니다. 이를 해결하기 위해 Spring WebFlux의 비동기/논블로킹 방식을 활용하거나, Tomcat 스레드 풀 크기를 튜닝하는 방법이 있습니다. 실제로 대규모 이벤트가 예상되면 서버를 수평 확장하고, Redis Pub/Sub으로 인스턴스 간 이벤트를 동기화해야 합니다. `NotificationSubscriber`가 Redis Pub/Sub을 구독해 이 구조를 지원합니다.

---

## 6. 추천 시스템 & 페르소나

**Q. 4가지 추천 전략을 어떻게 구분해서 적용했나요?**

> 상황과 데이터 가용성에 따라 다르게 적용했습니다.
> - **일간/주간 인기**: 모든 사용자에게 공통 적용, 스케줄 배치로 업데이트
> - **콘텐츠 기반 필터링**: 콘텐츠 상세 페이지의 "관련작 추천"에 적용, 장르·배우·줄거리 유사도 기반
> - **협업 필터링**: 로그인 사용자에게 취향이 유사한 다른 사용자가 좋아한 콘텐츠 추천
> - **Popcoism**: 필터 옵션으로 제공, 사용자가 선택적으로 활성화

---

**Q. 페르소나 점수는 어떻게 실시간으로 업데이트되나요?**

> `UserPersona` 테이블에 사용자별 페르소나 점수가 저장됩니다. 사용자가 리뷰를 작성하거나 좋아요/싫어요를 누를 때마다 해당 콘텐츠의 장르를 확인하고, 그 장르에 affinity가 높은 페르소나의 점수를 증가시킵니다. 점수가 가장 높은 페르소나가 메인 페르소나, 두 번째가 서브 페르소나가 됩니다. 배치 업데이트가 아닌 이벤트 드리븐 방식이라 즉시 반영됩니다.

---

**Q. AI 리뷰 요약에서 비용을 어떻게 관리했나요?**

> `ReviewSummary` 테이블에 생성된 요약을 캐시합니다. 처음 5개 리뷰가 쌓일 때 OpenAI API를 한 번 호출하고 결과를 저장합니다. 이후 새 리뷰가 추가될 때는 임계값 기준으로 재생성을 트리거하되, 너무 자주 호출되지 않도록 조건을 걸었습니다. 요약이 이미 있으면 DB에서 바로 반환해 API 비용을 최소화했습니다.

---

## 7. 성능 & 최적화

**Q. N+1 문제는 어떻게 해결했나요?**

> 주로 두 가지 방법을 사용했습니다. 첫째, JPA의 `fetch join`이나 `@EntityGraph`를 사용해 연관 엔티티를 한 번의 쿼리로 가져옵니다. 둘째, `@BatchSize`를 컬렉션에 적용해 IN 쿼리로 일괄 조회합니다. 예를 들어 콘텐츠 목록을 조회할 때 각 콘텐츠의 장르, 캐스트를 개별 쿼리로 조회하면 N+1이 발생하는데, BatchSize로 한 번에 묶어서 조회했습니다.

---

**Q. 페이지네이션은 어떤 방식으로 구현했나요?**

> Spring Data의 `Page<T>` 인터페이스를 사용한 offset 기반 페이지네이션을 사용했습니다. `?page=0&size=40` 방식입니다. 대용량 데이터에서 offset이 커질수록 성능이 떨어지는 문제가 있어, 커서 기반(keyset) 페이지네이션으로 개선하는 방법도 고려했습니다. 현재 데이터 규모에서는 offset 방식으로도 충분한 성능이 나왔습니다.

---

**Q. 쿼리 성능 문제가 발생했을 때 어떻게 디버깅했나요?**

> Spring Boot의 `spring.jpa.show-sql=true`와 `p6spy` 또는 Hibernate Statistics로 실행되는 쿼리를 확인했습니다. 실행 계획은 MySQL의 `EXPLAIN`으로 분석하고, 풀 스캔이 일어나는 경우 복합 인덱스를 추가했습니다. QueryDSL은 타입 세이프하게 쿼리를 작성할 수 있어 실수로 잘못된 쿼리가 생성되는 것을 컴파일 타임에 잡을 수 있었습니다.

---

## 8. 설계 결정 & 트레이드오프

**Q. 콘텐츠에 복합키(contentId + type)를 사용한 이유가 무엇인가요?**

> TMDB API에서 영화와 TV 시리즈가 같은 숫자 ID를 가지는 경우가 있습니다. 단순 `contentId`만 PK로 사용하면 영화 ID 12345와 TV ID 12345가 충돌합니다. 복합키 `(contentId, type)`을 사용하면 이 충돌 없이 두 타입을 동일한 테이블에서 관리할 수 있고, 모든 콘텐츠 API가 `types/{type}` 경로 파라미터를 받는 일관된 인터페이스를 유지할 수 있습니다.

---

**Q. 리뷰를 실제로 삭제하지 않고 status 필드로 관리한 이유는요?**

> 소프트 삭제(soft delete)를 선택한 이유는 세 가지입니다. 첫째, 신고된 리뷰를 관리자가 검토하고 복구할 수 있어야 합니다. 둘째, 사용자가 실수로 삭제한 리뷰를 복구 요청할 때 대응 가능합니다. 셋째, 삭제된 리뷰 데이터도 통계나 AI 학습에 활용할 수 있습니다. 쿼리 시 `WHERE status = 'ACTIVE'` 조건을 항상 포함해야 하는 번거로움이 있지만, 데이터 보존의 이점이 더 크다고 판단했습니다.

---

**Q. Service Layer를 Interface + Impl로 분리한 이유가 있나요?**

> 두 가지 이점을 위해서입니다. 첫째, 테스트 용이성입니다. 인터페이스를 기반으로 Mock 객체를 쉽게 만들어 단위 테스트에서 의존성을 주입할 수 있습니다. 둘째, 다형성입니다. 예를 들어 `GptService` 인터페이스를 두고 OpenAI 구현체와 Gemini 구현체를 전환 가능하게 설계할 수 있습니다. 실제로 AI 요약 기능에서 OpenAI와 Gemini를 상황에 따라 선택할 수 있도록 이 구조가 활용되었습니다.

---

## 9. Spring & JPA 심화

**Q. @Transactional을 어디에 적용했나요? 주의할 점은요?**

> Service 메서드에 적용했습니다. 주의한 점은 두 가지입니다. 첫째, 읽기 전용 메서드에는 `@Transactional(readOnly = true)`를 사용해 성능을 최적화했습니다 — Hibernate의 dirty checking을 생략하고 DB 복제 환경에서 read replica로 라우팅이 가능합니다. 둘째, `@Transactional`이 적용된 메서드를 같은 클래스 내부에서 호출하면 프록시를 거치지 않아 트랜잭션이 적용되지 않는 문제(self-invocation)가 있어, 이 경우 별도 빈으로 분리하거나 구조를 바꾸었습니다.

---

**Q. QueryDSL을 JPA Criteria API 대신 선택한 이유는요?**

> QueryDSL이 훨씬 가독성이 좋기 때문입니다. Criteria API는 메서드 체인이 복잡하고 코드가 장황해서 비즈니스 의도가 묻힙니다. QueryDSL은 SQL에 가까운 직관적인 문법으로 작성할 수 있고, Q클래스를 통해 컴파일 타임에 필드 이름 오타를 잡을 수 있습니다. 특히 콘텐츠 필터링처럼 조건이 동적으로 조합되는 쿼리에서 `BooleanBuilder`를 사용해 깔끔하게 처리했습니다.

---

**Q. JPA 즉시 로딩(EAGER)과 지연 로딩(LAZY) 차이와 선택 기준은요?**

> 기본적으로 모든 연관관계에 `LAZY`를 적용했습니다. `EAGER`는 엔티티를 로드할 때 항상 연관 데이터를 가져오는데, 사용하지 않는 데이터까지 조회해 성능 낭비가 생깁니다. `LAZY`는 실제 접근 시점에 쿼리가 실행되어 필요한 경우에만 조회합니다. 단, LAZY 로딩은 트랜잭션 밖에서 접근하면 `LazyInitializationException`이 발생하므로, 필요한 연관 데이터는 Service 레이어 트랜잭션 안에서 fetch join으로 명시적으로 가져왔습니다.

---

**Q. @EnableScheduling은 어떻게 활용했나요?**

> `PopcoClientApplication`에 `@EnableScheduling`을 선언하고, 스케줄 컴포넌트에서 `@Scheduled`로 작업을 등록했습니다.
> - **일간 인기 콘텐츠 업데이트**: 자정에 배치 실행
> - **주간 트렌딩 리뷰 집계**: 매주 월요일 새벽 실행
> - **퀴즈 이벤트 진행**: 이벤트 시작 시간과 문제 전환 타이밍에 스케줄 실행
> 
> 다만 단일 서버에서만 안전하고, 스케일 아웃 시 중복 실행 문제가 생깁니다. 이를 위해 Redis 분산 락(`DistributeLockServiceImpl`)을 함께 사용해 한 인스턴스만 실행되도록 보장했습니다.

---

## 10. 장애 대응 & 트러블슈팅

**Q. 서비스 운영 중 발생한 버그나 장애 경험이 있나요?**

> *(본인 경험에 맞게 조정하세요 — 아래는 예시입니다)*
>
> 퀴즈 이벤트 테스트 중 동시 제출 시 순위가 뒤바뀌는 문제가 있었습니다. 원인은 "카운터 조회 → 증가 → 저장" 사이에 다른 요청이 끼어드는 Race Condition이었습니다. Redis의 INCR 명령과 Lua 스크립트로 이 과정을 원자적으로 묶어 해결했습니다. 이후 JMeter로 동시 100건 요청 테스트를 통해 순위 정합성을 검증했습니다.

---

**Q. Elasticsearch 인덱스가 MySQL과 데이터 불일치가 생기면 어떻게 하나요?**

> 어드민 전용 `POST /index/**` 엔드포인트로 전체 재인덱싱을 트리거할 수 있습니다. `ContentMigrationService`가 MySQL의 모든 콘텐츠를 읽어 ES 인덱스를 다시 빌드합니다. 불일치는 주로 배포나 서버 재시작 과정에서 발생할 수 있어, 배포 후 재인덱싱을 운영 체크리스트에 포함시켰습니다. 장기적으로는 변경 이벤트를 기반으로 자동 동기화하는 CDC(Change Data Capture) 방식이 더 안정적입니다.

---

**Q. 외부 API(OpenAI, Kakao) 호출 실패 시 어떻게 처리했나요?**

> `GlobalExceptionHandler`에서 외부 API 예외를 잡아 사용자에게 적절한 에러 메시지를 반환합니다. AI 요약은 실패해도 서비스의 핵심 기능이 아니므로, 실패 시 요약 없이 리뷰 목록만 반환하는 graceful degradation을 적용했습니다. Kakao 로그인 실패는 사용자에게 재시도를 안내합니다. WebClient의 timeout 설정으로 외부 API 응답 지연이 전체 서비스에 영향을 주지 않도록 했습니다.

---

## 자주 나오는 꼬리 질문 대비

| 주제 | 꼬리 질문 예시 |
|------|----------------|
| JWT | "토큰 탈취 시나리오를 구체적으로 설명해 보세요" |
| Redis | "Redis가 다운되면 서비스가 어떻게 되나요?" |
| ES | "ES 인덱스 매핑을 잘못 설계하면 어떤 문제가 생기나요?" |
| SSE | "100만 명 동시 접속이면 어떻게 확장하겠습니까?" |
| 페르소나 | "추천 알고리즘의 정확도는 어떻게 측정했나요?" |
| N+1 | "BatchSize를 너무 크게 잡으면 어떤 문제가 있나요?" |
| 트랜잭션 | "분산 트랜잭션이 필요한 상황이 있었나요?" |

---

## 면접 전 체크리스트

- [ ] 내가 직접 구현한 코드 파일 3~5개 다시 읽기
- [ ] 가장 어려웠던 문제와 해결 과정 STAR 형식으로 정리
- [ ] 기술 선택 이유를 "대안 대비 장단점" 형태로 설명 연습
- [ ] 프로젝트 전체 아키텍처를 화이트보드 없이 말로 설명 연습
- [ ] 개선하고 싶은 점 2~3가지 준비 (단순 비판이 아닌 구체적 방안 포함)