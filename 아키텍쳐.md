# Popco 아키텍처 설계도

> 이 문서는 Claude가 프로젝트 전체 구조를 한 번에 파악하기 위한 참조 지도다.
> 코드를 탐색하기 전에 이 파일을 먼저 읽으면 토큰을 아낄 수 있다.

---

## 1. 전체 시스템 다이어그램

```
외부 인터넷
      │
      ▼ :80 (유일한 외부 노출 포트)
┌─────────────────────────────────────────────────────────────┐
│                        Nginx                                │
│  (Reverse Proxy + Static File Server + API Gateway)        │
│                                                             │
│  / ──────────────────────────→ [정적 파일] React 빌드      │
│  /api/admin/* ───────────────→ popco-admin:8080            │
│  /api/client/* ──────────────→ popco-client:8080           │
│  /api/client/ws ─────────────→ popco-client:8080 (WS)     │
│  /api/client/notifications/  │                             │
│    stream ───────────────────→ popco-client:8080 (SSE)    │
└──────────────┬───────────────┬─────────────────────────────┘
               │               │
               ▼               ▼
    ┌──────────────┐  ┌─────────────────┐
    │ Popco-Admin  │  │  Popco-Client   │
    │ Spring Boot  │  │  Spring Boot    │
    │ Java 17      │  │  Java 17        │
    │ :8081→8080   │  │  :8082→8080     │
    └──────┬───────┘  └────────┬────────┘
           │                   │ HTTP 직접 호출
           │                   ▼
           │         ┌─────────────────────┐
           │         │  Popco-Recommended  │
           │         │  FastAPI (Python)   │
           │         │  :8084→8000         │
           │         └────────┬────────────┘
           │                  │
           └──────────┬───────┘
                      │
          ┌───────────┼────────────────┐
          ▼           ▼                ▼
       [MySQL]     [Redis]     [Elasticsearch]
       :3306       :6379           :9200
    (prod: RDS)  (캐시/세션)    (전문 검색)
```

---

## 2. 리포지토리 & 디렉토리 구조

```
C:\Users\USER\Coding_workspace\popco\
│
├── Popco-Infra\                     ← 인프라 설정 허브
│   ├── docker-compose.yml           ← 로컬 개발: MySQL+Redis+ES 포함
│   ├── docker-compose-prod.yml      ← 프로덕션: RDS 사용, MySQL 제외
│   ├── .env.example                 ← 환경변수 템플릿
│   ├── mysql\init\                  ← DB 초기 스크립트
│   ├── elasticsearch\Dockerfile     ← 커스텀 ES 이미지
│   └── .github\workflows\
│       ├── docker-cicd.yml          ← compose 문법 검사
│       └── deploy.yml               ← EC2 SSH 배포
│
├── Popco-Admin\                     ← 관리자 백엔드
│   ├── build.gradle
│   ├── Dockerfile
│   └── src\main\java\...\
│       ├── content\                 ← 영상 콘텐츠 CRUD + 배치
│       ├── persona\                 ← 페르소나 관리
│       ├── quiz\                    ← 퀴즈 기능
│       ├── review\                  ← 리뷰 분석 (Gemini API)
│       ├── scheduler\               ← 정기 작업
│       └── apiclient\               ← 외부 API 연동
│
├── Popco-Client_Fork\               ← 사용자 API 백엔드 (현재 작업 디렉토리)
│   ├── build.gradle
│   ├── Dockerfile
│   └── src\main\java\...\
│       ├── auth\                    ← JWT + Kakao OAuth2
│       ├── content\                 ← 콘텐츠 검색/조회 (ES)
│       ├── user\                    ← 사용자 관리
│       ├── chat\                    ← WebSocket 채팅
│       ├── collection\              ← 컬렉션/위시리스트
│       ├── event\                   ← 이벤트
│       ├── review\                  ← 리뷰 조회/작성
│       ├── persona\                 ← 페르소나 기능
│       ├── declaration\             ← 신고
│       └── redis\                   ← Redis 통합
│
├── Popco-Recommended\               ← AI 추천 엔진
│   ├── requirements.txt
│   ├── Dockerfile
│   └── app\
│       ├── content_based_recommender\   ← BERT 기반 콘텐츠 필터링
│       ├── user_based_recommender\      ← 협업 필터링
│       ├── persona_based_recommender\   ← 페르소나 추천
│       ├── popcorithm\                  ← 커스텀 추천 알고리즘
│       ├── data_processing\             ← 전처리
│       └── filtering\                   ← 추천 필터
│
└── Popco-Front\                     ← React 프론트엔드
    ├── nginx.conf                   ← ★ Nginx 핵심 설정 파일
    ├── Dockerfile
    ├── vite.config.ts
    └── src\
        ├── pages\                   ← MainPage, ListPage, DetailPage,
        │                               MyPage, EventPage, LoginRegisterPage
        ├── components\              ← UI 컴포넌트
        ├── hooks\                   ← React Query hooks
        ├── store\                   ← Redux + Zustand 상태관리
        ├── apis\                    ← Axios API 통신 레이어
        └── routes\                  ← SPA 라우팅
```

---

## 3. Nginx 라우팅 상세 (핵심 파일: Popco-Front/nginx.conf)

```
server {
  listen 80;
  server_name popco.site;

  # 프론트엔드 정적 서빙
  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;   ← SPA fallback
  }

  # 관리자 API 프록시
  location /api/admin/ {
    proxy_pass http://popco-admin:8080/;
  }

  # 사용자 API 프록시 (일반 REST)
  location /api/client/ {
    proxy_pass http://popco-client:8080/;
    # CORS 헤더 추가
    add_header Access-Control-Allow-Origin *;
  }

  # WebSocket 업그레이드
  location /api/client/ws {
    proxy_pass http://popco-client:8080/ws;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }

  # SSE 스트리밍 (버퍼링 OFF 필수)
  location /api/client/notifications/stream {
    proxy_pass http://popco-client:8080/;
    proxy_buffering off;
    proxy_read_timeout 86400s;   ← 24시간 연결 유지
  }
}

# 부가 설정
- gzip 압축: ON (html/css/js/json)
- 정적 자산 캐싱: 1년 (Cache-Control: max-age=31536000)
- 보안 헤더: X-Frame-Options DENY, X-XSS-Protection, X-Content-Type-Options nosniff
```

---

## 4. 서비스별 기술 스택 & 포트

| 서비스 | 언어/프레임워크 | 컨테이너 포트 | 로컬 호스트 포트 | 주요 의존성 |
|--------|----------------|--------------|----------------|------------|
| Nginx | - | 80 | 80 | - |
| Popco-Admin | Java 17 / Spring Boot 3.5.3 | 8080 | 8081 | MySQL, Redis, OpenAI, Gemini |
| Popco-Client | Java 17 / Spring Boot 3.5.3 | 8080 | 8082 | MySQL, Redis, ES, S3, Kakao OAuth |
| Popco-Recommended | Python 3.11 / FastAPI | 8000 | 8084 | MySQL, Redis, S3, PyTorch |
| MySQL | - | 3306 | 3307 (로컬) / RDS (prod) | - |
| Redis | - | 6379 | 6379 | - |
| Elasticsearch | 8.x | 9200 | 9200 | - |
| Kibana | - | 5601 | 5601 | - |

---

## 5. 서비스 간 통신 지도

```
Popco-Front (브라우저)
  │
  ├─ Axios HTTP ──→ Nginx → /api/client/* → Popco-Client
  ├─ Axios HTTP ──→ Nginx → /api/admin/* → Popco-Admin
  ├─ WebSocket ───→ Nginx → /api/client/ws → Popco-Client
  └─ SSE ─────────→ Nginx → /api/client/notifications/stream → Popco-Client

Popco-Client
  ├─ HTTP ───────────────────────────────→ Popco-Recommended:8000 (내부 직접 호출)
  ├─ JPA/JDBC ──────────────────────────→ MySQL
  ├─ RedisTemplate ─────────────────────→ Redis
  ├─ ElasticsearchClient ───────────────→ Elasticsearch
  └─ S3Client ──────────────────────────→ AWS S3

Popco-Admin
  ├─ JPA/JDBC ──────────────────────────→ MySQL (Client와 동일 DB)
  ├─ RedisTemplate ─────────────────────→ Redis
  ├─ HTTP (OpenAI API) ─────────────────→ api.openai.com
  └─ HTTP (Gemini API) ─────────────────→ generativelanguage.googleapis.com

Popco-Recommended
  ├─ SQLAlchemy ────────────────────────→ MySQL
  ├─ redis-py ──────────────────────────→ Redis
  └─ boto3 ─────────────────────────────→ AWS S3
```

---

## 6. 데이터 계층 상세

```
MySQL (공유 DB)
  └─ Admin과 Client가 동일 스키마 사용
  └─ 주요 도메인 테이블: users, contents, reviews, personas,
                         quizzes, collections, events, declarations, chats

Redis (공유 캐시)
  └─ 용도: JWT 블랙리스트, 세션, API 응답 캐시, Pub/Sub (알림)
  └─ AOF 영속화 활성화

Elasticsearch
  └─ Client 전용 검색 인덱스
  └─ 콘텐츠 전문 검색 (제목, 설명, 태그)
  └─ QueryDSL로 필터링
```

---

## 7. Popco-Client 도메인 모듈 상세

```
auth/
  ├─ JWT 발급/검증/갱신/블랙리스트(Redis)
  ├─ Kakao OAuth2 소셜 로그인
  └─ Spring Security 필터 체인

content/
  ├─ Elasticsearch 전문 검색
  ├─ 카테고리/장르/태그 필터링
  └─ QueryDSL 복합 조건 쿼리

chat/
  ├─ STOMP over WebSocket
  └─ 실시간 채팅방

user/ → 프로필, 팔로우
collection/ → 위시리스트, 컬렉션 CRUD
event/ → 이벤트 조회/참여
review/ → 별점, 텍스트 리뷰
persona/ → 취향 페르소나 기반 추천 연동
declaration/ → 콘텐츠/댓글 신고
redis/ → RedisTemplate 빈 설정, Pub/Sub 구독
```

---

## 8. 배포 파이프라인

```
개발자 Push → GitHub
      │
      ▼
GitHub Actions (Popco-Infra/.github/workflows/)
  ├─ docker-cicd.yml
  │   └─ docker-compose 문법 검사 + 환경변수 일관성 확인
  │
  └─ deploy.yml
      ├─ SSH → EC2
      ├─ git pull (Popco-Infra)
      ├─ docker-compose down
      ├─ docker pull (Docker Hub 이미지)
      ├─ docker-compose -f docker-compose-prod.yml up -d
      └─ 헬스체크 자동화
```

---

## 9. 환경변수 주요 키 (Popco-Infra/.env.example 기준)

```
# DB
MYSQL_HOST, MYSQL_PORT, MYSQL_DATABASE, MYSQL_USER, MYSQL_PASSWORD

# Redis
REDIS_HOST, REDIS_PORT

# Elasticsearch
ES_HOST, ES_PORT

# JWT
JWT_SECRET, JWT_EXPIRATION, JWT_REFRESH_EXPIRATION

# OAuth
KAKAO_CLIENT_ID, KAKAO_REDIRECT_URI

# AWS
AWS_ACCESS_KEY, AWS_SECRET_KEY, S3_BUCKET_NAME, S3_REGION

# External API
OPENAI_API_KEY
GEMINI_API_KEY

# Service URLs (서비스 간 통신)
RECOMMENDED_SERVICE_URL=http://popco-recommended:8000
```

---

## 10. MSA 판단 기준 요약

```
같은 EC2 한 대에서 돌아도 MSA인 이유:

독립 코드베이스  ✓  각자 별도 Git 리포
독립 빌드/배포  ✓  서비스별 Dockerfile + 독립 CI/CD
폴리글랏        ✓  Java (Admin/Client) + Python (Recommended)
컨테이너 격리   ✓  Docker로 프로세스 격리, 하나 죽어도 나머지 생존
단일 책임       ✓  Admin=관리, Client=사용자, Recommended=AI 추천
독립 스케일링   ✓  Client 컨테이너만 늘리기 가능 (Nginx upstream 추가)

Nginx = 물리적 단일 진입점이지 MSA 여부를 결정하는 요소가 아님
        → API Gateway 패턴으로, MSA에서 표준적으로 사용
```

---

## 11. 주요 요청 흐름 (한눈에)

```
[콘텐츠 검색]
브라우저 → Nginx /api/client/contents → Client → Elasticsearch → MySQL → JSON

[추천]
브라우저 → Nginx /api/client/recommend → Client → Recommended:8000 → PyTorch → JSON

[로그인]
브라우저 → Nginx /api/client/auth/login → Client → MySQL → JWT 발급 → Redis 저장

[실시간 알림]
브라우저 → Nginx /api/client/notifications/stream (SSE, 24h 유지)
  ← Client ← Redis Pub/Sub (이벤트 발생 시 push)

[채팅]
브라우저 ↔ Nginx /api/client/ws (WebSocket upgrade) ↔ Client STOMP 브로커

[관리자 콘텐츠 등록]
어드민 브라우저 → Nginx /api/admin/contents → Admin → MySQL + S3
```